

// ContentView - BEGINNING

//
//  ContentView.swift
//  to do
//
//  Created by Ingrid Zippe on 6/24/26.
//

import SwiftUI
import SwiftData

struct ContentView: View {
    @Environment(\.modelContext) private var modelContext
    @Query private var items: [Item]

    var body: some View {
        NavigationSplitView {
            List {
                ForEach(items) { item in
                    NavigationLink {
                        Text("Item at \(item.timestamp, format: Date.FormatStyle(date: .numeric, time: .standard))")
                    } label: {
                        Text(item.timestamp, format: Date.FormatStyle(date: .numeric, time: .standard))
                    }
                }
                .onDelete(perform: deleteItems)
            }
            .toolbar {
                ToolbarItem(placement: .navigationBarTrailing) {
                    EditButton()
                }
                ToolbarItem {
                    Button(action: addItem) {
                        Label("Add Item", systemImage: "plus")
                    }
                }
            }
        } detail: {
            Text("Select an item")
        }
    }

    private func addItem() {
        withAnimation {
            let newItem = Item(timestamp: Date())
            modelContext.insert(newItem)
        }
    }

    private func deleteItems(offsets: IndexSet) {
        withAnimation {
            for index in offsets {
                modelContext.delete(items[index])
            }
        }
    }
}

#Preview {
    ContentView()
        .modelContainer(for: Item.self, inMemory: true)
}


// ContentView - END

// Item - BEGINNING

//
//  Item.swift
//  to do
//
//  Created by Ingrid Zippe on 6/24/26.
//

import Foundation
import SwiftData

@Model
final class Item {
    var timestamp: Date
    
    init(timestamp: Date) {
        self.timestamp = timestamp
    }
}


// Item - END

// working - BEGINNING - text editor

import SwiftUI
import SwiftData

struct ContentView: View {
    @Environment(\.modelContext) private var modelContext
    @Query private var items: [Item]
    @State private var fullText: String = "This is some editable text..."

    var body: some View {
        NavigationSplitView {
            List {
                ForEach(items) { item in
                    NavigationLink {
                        Text("Item at \(item.timestamp)")
                    } label: {
                        TextEditor(text: $fullText)
                    }
                }
                .onDelete(perform: deleteItems)
            }
            .toolbar {
                ToolbarItem(placement: .navigationBarTrailing) {
                    EditButton()
                }
                ToolbarItem {
                    Button(action: addItem) {
                        Label("Add Item", systemImage: "plus")
                    }
                }
            }
        } detail: {
            Text("Select an item")
        }
    }

    private func addItem() {
        withAnimation {
            let newItem = Item(timestamp: Date())
            modelContext.insert(newItem)
        }
    }

    private func deleteItems(offsets: IndexSet) {
        withAnimation {
            for index in offsets {
                modelContext.delete(items[index])
            }
        }
    }
}

#Preview {
    ContentView()
        .modelContainer(for: Item.self, inMemory: true)
}

// working - END

// working - BEGINNING - added ToDo to @Model

import SwiftUI
import SwiftData

struct ContentView: View {
    @Environment(\.modelContext) private var modelContext
    @Query private var items: [Item]
    @State private var fullText: String = "This is some editable text..."
    let toDoList = [""]

    var body: some View {
        NavigationSplitView {
            List {
                ForEach(items) { item in
                    NavigationLink {
                        Text("Item at \(item.timestamp)")
                    } label: {
                        TextEditor(text: $fullText)
                    }
                }
                .onDelete(perform: deleteItems)
            }
            .toolbar {
                ToolbarItem(placement: .navigationBarTrailing) {
                    EditButton()
                }
                ToolbarItem {
                    Button(action: addItem) {
                        Label("Add Item", systemImage: "plus")
                    }
                }
            }
        } detail: {
            Text("Select an item")
        }
    }

    private func addItem() {
        withAnimation {
            let newItem = Item(timestamp: Date(), toDo: "string")
            modelContext.insert(newItem)
        }
    }

    private func deleteItems(offsets: IndexSet) {
        withAnimation {
            for index in offsets {
                modelContext.delete(items[index])
            }
        }
    }
}

#Preview {
    ContentView()
        .modelContainer(for: Item.self, inMemory: true)
}

// working - END

// working - BEGINNING

import Foundation
import SwiftData

@Model
final class Item {
    var timestamp: Date
    var toDo: String
    
    init(timestamp: Date, toDo: String) {
        self.timestamp = timestamp
        self.toDo = toDo
    }
}

// working - END

// working - BEGINNING - toDoList printing

import SwiftUI
import SwiftData

struct ContentView: View {
    @Environment(\.modelContext) private var modelContext
    @Query private var items: [Item]
    @State private var fullText: String = "This is some editable text..."
    @State private var toDoList: [String] = ["to do 1", "to do 2"]
//    let toDoList = [""]

    var body: some View {
        NavigationSplitView {
            List {
                ForEach(items) { item in
                    NavigationLink {
                        Text("Item at \(item.timestamp)")
                    } label: {
                        TextEditor(text: $toDoList[0])
                    }
                }
                .onDelete(perform: deleteItems)
            }
            .toolbar {
                ToolbarItem(placement: .navigationBarTrailing) {
                    EditButton()
                }
                ToolbarItem {
                    Button(action: addItem) {
                        Label("Add Item", systemImage: "plus")
                    }
                }
            }
        } detail: {
            Text("Select an item")
        }
    }

    private func addItem() {
        withAnimation {
            let newItem = Item(timestamp: Date(), toDo: "string")
            modelContext.insert(newItem)
        }
    }

    private func deleteItems(offsets: IndexSet) {
        withAnimation {
            for index in offsets {
                modelContext.delete(items[index])
            }
        }
    }
}

#Preview {
    ContentView()
        .modelContainer(for: Item.self, inMemory: true)
}

// working - END

// working - BEGINNING - lists ToDo items 

import SwiftUI
import SwiftData

struct ContentView: View {
    @Environment(\.modelContext) private var modelContext
    @Query private var items: [Item]
    @State private var fullText: String = "This is some editable text..."
    @State private var toDoList: [String] = ["to do 1", "to do 2"]
//    let toDoList = [""]

    var body: some View {
        NavigationSplitView {
            List {
                ForEach(Array(toDoList.enumerated()), id: \.offset) { index, toDo in
                                HStack {
                                    Text("Index: \(index)") // 0, 1, 2, 3
                                        .foregroundColor(.gray)
                                    Spacer()
                                    Text(toDo)
                                }
                            }
//                ForEach(items) { item in
//                    NavigationLink {
//                        Text("Item at \(item.timestamp)")
//                    } label: {
//                        TextEditor(text: $toDoList[toDoList.count])
//                    }
//                }
                .onDelete(perform: deleteItems)
            }
            .toolbar {
                ToolbarItem(placement: .navigationBarTrailing) {
                    EditButton()
                }
                ToolbarItem {
                    Button(action: addItem) {
                        Label("Add Item", systemImage: "plus")
                    }
                }
            }
        } detail: {
            Text("Select an item")
        }
    }

    private func addItem() {
        withAnimation {
            let newItem = Item(timestamp: Date(), toDo: "string")
            modelContext.insert(newItem)
        }
    }

    private func deleteItems(offsets: IndexSet) {
        withAnimation {
            for index in offsets {
                modelContext.delete(items[index])
            }
        }
    }
}

#Preview {
    ContentView()
        .modelContainer(for: Item.self, inMemory: true)
}

// working - END

// workign - BEGINNING - add item to toDoList

import SwiftUI
import SwiftData

struct ContentView: View {
    @Environment(\.modelContext) private var modelContext
    @Query private var items: [Item]
    @State private var fullText: String = "This is some editable text..."
    @State private var toDoList: [String] = ["to do 1", "to do 2"]
//    let toDoList = [""]

    var body: some View {
        NavigationSplitView {
            List {
                ForEach(Array(toDoList.enumerated()), id: \.offset) { index, toDo in
                                HStack {
                                    Text("Index: \(index)") // 0, 1, 2, 3
                                        .foregroundColor(.gray)
                                    Spacer()
                                    Text(toDo)
                                }
                            }
//                ForEach(items) { item in
//                    NavigationLink {
//                        Text("Item at \(item.timestamp)")
//                    } label: {
//                        TextEditor(text: $toDoList[toDoList.count])
//                    }
//                }
                .onDelete(perform: deleteItems)
            }
            .toolbar {
                ToolbarItem(placement: .navigationBarTrailing) {
                    EditButton()
                }
                ToolbarItem {
                    Button(action: addItem2) {
                        Label("Add Item", systemImage: "plus")
                    }
                }
            }
        } detail: {
            Text("Select an item")
        }
    }

    private func addItem() {
        withAnimation {
            let newItem = Item(timestamp: Date(), toDo: "string")
            modelContext.insert(newItem)
        }
    }
    private func addItem2() {
        withAnimation {
            toDoList.append("to do 3")
        }
    }

    private func deleteItems(offsets: IndexSet) {
        withAnimation {
            for index in offsets {
                modelContext.delete(items[index])
            }
        }
    }
}

#Preview {
    ContentView()
        .modelContainer(for: Item.self, inMemory: true)
}
// working - END

// working - BEGINNING - delete items at index

import SwiftUI
import SwiftData

struct ContentView: View {
    @Environment(\.modelContext) private var modelContext
    @Query private var items: [Item]
    @State private var fullText: String = "This is some editable text..."
    @State private var toDoList: [String] = ["to do 1", "to do 2"]
//    let toDoList = [""]

    var body: some View {
        NavigationSplitView {
            List {
                ForEach(Array(toDoList.enumerated()), id: \.offset) { index, toDo in
                                HStack {
                                    Text("Index: \(index)") // 0, 1, 2, 3
                                        .foregroundColor(.gray)
                                    Spacer()
                                    Text(toDo)
                                }
                            }
//                ForEach(items) { item in
//                    NavigationLink {
//                        Text("Item at \(item.timestamp)")
//                    } label: {
//                        TextEditor(text: $toDoList[toDoList.count])
//                    }
//                }
                .onDelete { offsets in
                    deleteItems3(at: offsets)
                }
            }
            .toolbar {
                ToolbarItem(placement: .navigationBarTrailing) {
                    EditButton()
                }
                ToolbarItem {
                    Button(action: addItem2) {
                        Label("Add Item", systemImage: "plus")
                    }
                }
            }
        } detail: {
            Text("Select an item")
        }
    }

    private func addItem() {
        withAnimation {
            let newItem = Item(timestamp: Date(), toDo: "string")
            modelContext.insert(newItem)
        }
    }
    private func addItem2() {
        withAnimation {
            toDoList.append("to do 3")
        }
    }

    private func deleteItems(offsets: IndexSet) {
        withAnimation {
            for index in offsets {
                modelContext.delete(items[index])
            }
        }
    }
    private func deleteItems2(int index: Int) {
        withAnimation {
            toDoList.remove(at: index)
        }
    }
    private func deleteItems3(at offsets: IndexSet) {
            toDoList.remove(atOffsets: offsets)
    }
}

#Preview {
    ContentView()
        .modelContainer(for: Item.self, inMemory: true)
}

// working - END

// working - BEGINNING 

import SwiftUI
import SwiftData

struct ContentView: View {
    @Environment(\.modelContext) private var modelContext
    @Query private var items: [Item]
    @State private var fullText: String = "This is some editable text..."
    @State private var toDoList: [String] = ["to do 1", "to do 2"]
//    let toDoList = [""]

    var body: some View {
        NavigationSplitView {
            List {
                ForEach(Array(toDoList.enumerated()), id: \.offset) { index, toDo in
                                HStack {
                                    Text("Index: \(index)") // 0, 1, 2, 3
                                        .foregroundColor(.gray)
                                    Spacer()
                                    Text(toDo)
                                }
                            }
//                ForEach(items) { item in
//                    NavigationLink {
//                        Text("Item at \(item.timestamp)")
//                    } label: {
//                        TextEditor(text: $toDoList[toDoList.count])
//                    }
//                }
                .onDelete { offsets in
                    deleteItems3(at: offsets)
                }
            }
            .toolbar {
                ToolbarItem(placement: .navigationBarTrailing) {
                    EditButton()
                }
                ToolbarItem {
                    Button(action: addItem2) {
                        Label("Add Item", systemImage: "plus")
                    }
                }
            }
        } detail: {
            Text("Select an item")
        }
    }

    private func addItem() {
        withAnimation {
            let newItem = Item(timestamp: Date(), toDo: "string")
            modelContext.insert(newItem)
        }
    }
    private func addItem2() {
        withAnimation {
            toDoList.append("to do 3")
        }
    }

    private func deleteItems(offsets: IndexSet) {
        withAnimation {
            for index in offsets {
                modelContext.delete(items[index])
            }
        }
    }
    private func deleteItems2(int index: Int) {
        withAnimation {
            toDoList.remove(at: index)
        }
    }
    private func deleteItems3(at offsets: IndexSet) {
            toDoList.remove(atOffsets: offsets)
    }
}

#Preview {
    ContentView()
        .modelContainer(for: Item.self, inMemory: true)
}
// working - END

// working - BEGINNING 

import Foundation
import SwiftData

@Model
final class Item {
    var timestamp: Date
    var toDo: String
    
    init(timestamp: Date, toDo: String) {
        self.timestamp = timestamp
        self.toDo = toDo
    }
}


// working - END

// working - BEGINNING

@main
struct to_doApp: App {
    var sharedModelContainer: ModelContainer = {
        let schema = Schema([
            Item.self,
        ])
        let modelConfiguration = ModelConfiguration(schema: schema, isStoredInMemoryOnly: false)

        do {
            return try ModelContainer(for: schema, configurations: [modelConfiguration])
        } catch {
            fatalError("Could not create ModelContainer: \(error)")
        }
    }()

    var body: some Scene {
        WindowGroup {
            ContentView()
        }
        .modelContainer(sharedModelContainer)
    }
}


// working - END

// workig - BEGINNING - without add or delete

import SwiftUI
import SwiftData

struct ContentView: View {
    // 1. Get access to the database context for writing data
    @Environment(\.modelContext) private var modelContext
    
    // 2. Automatically fetch and sort items from the database
    @Query(sort: \TodoItem.createdAt, order: .reverse) private var items: [TodoItem]
    
    @State private var newTitle = ""
    
    var body: some View {
        NavigationStack {
            List {
                // Add New Item Section
                HStack {
                    TextField("New task...", text: $newTitle)
                    Button("Add") {
//                        addItem()
                    }
                    .disabled(newTitle.isEmpty)
                }
                
                // Display Items From Database
                ForEach(items) { item in
                    HStack {
                        Text(item.title)
                        Spacer()
                        if item.isCompleted {
                            Image(systemName: "checkmark.circle.fill")
                                .foregroundColor(.green)
                        }
                    }
                }
//                .onDelete(perform: deleteItems) // Swipe-to-delete
            }
            .navigationTitle("My Database List")
        }
    }
    
//    // 3. Insert data into the database
//    private var addItem() {
//        let newItem = TodoItem(title: newTitle)
//        modelContext.insert(newItem) // Saved to disk automatically
//        newTitle = "" // Clear text field
//    }
//    
//    // 4. Delete data from the database
//    private var deleteItems(at offsets: IndexSet) {
//        for index in offsets {
//            let itemToDelete = items[index]
//            modelContext.delete(itemToDelete) // Removed from disk
//        }
//    }
}


//import SwiftUI
//import SwiftData
//
//
//
//struct ContentView: View {
//    @Environment(\.modelContext) private var modelContext
//    @Query private var items: [Item]
//    @State private var fullText: String = "This is some editable text..."
//    @State private var toDoList: [String] = ["to do 1", "to do 2"]
////    let toDoList = [""]
//
//    var body: some View {
//        NavigationSplitView {
//            List {
//                ForEach(Array(toDoList.enumerated()), id: \.offset) { index, toDo in
//                                HStack {
//                                    Text("Index: \(index)") // 0, 1, 2, 3
//                                        .foregroundColor(.gray)
//                                    Spacer()
//                                    Text(toDo)
//                                }
//                            }
////                ForEach(items) { item in
////                    NavigationLink {
////                        Text("Item at \(item.timestamp)")
////                    } label: {
////                        TextEditor(text: $toDoList[toDoList.count])
////                    }
////                }
//                .onDelete { offsets in
//                    deleteItems3(at: offsets)
//                }
//            }
//            .toolbar {
//                ToolbarItem(placement: .navigationBarTrailing) {
//                    EditButton()
//                }
//                ToolbarItem {
//                    Button(action: addItem2) {
//                        Label("Add Item", systemImage: "plus")
//                    }
//                }
//            }
//        } detail: {
//            Text("Select an item")
//        }
//    }
//
//    private func addItem() {
//        withAnimation {
//            let newItem = Item(timestamp: Date(), toDo: "string")
//            modelContext.insert(newItem)
//        }
//    }
//    private func addItem2() {
//        withAnimation {
//            toDoList.append("to do 3")
//        }
//    }
//
//    private func deleteItems(offsets: IndexSet) {
//        withAnimation {
//            for index in offsets {
//                modelContext.delete(items[index])
//            }
//        }
//    }
//    private func deleteItems2(int index: Int) {
//        withAnimation {
//            toDoList.remove(at: index)
//        }
//    }
//    private func deleteItems3(at offsets: IndexSet) {
//            toDoList.remove(atOffsets: offsets)
//    }
//}
//
//#Preview {
//    ContentView()
//        .modelContainer(for: Item.self, inMemory: true)
//}

// working - END

// working - BEGINNING 
import Foundation
import SwiftData

@Model
class TodoItem {
    var title: String
    var isCompleted: Bool
    var createdAt: Date
    
    init(title: String, isCompleted: Bool = false, createdAt: Date = .now) {
        self.title = title
        self.isCompleted = isCompleted
        self.createdAt = createdAt
    }
}
// working - END

// working - BEGINNING 

import SwiftUI
import SwiftData

@main
struct TodoApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
        // Tell SwiftData to manage the database container for your models
        .modelContainer(for: TodoItem.self)
    }
}
// working - END

// working - BEGINNING - database working for addItem()

import SwiftUI
import SwiftData

struct ContentView: View {
    // 1. Get access to the database context for writing data
    @Environment(\.modelContext) private var modelContext
    
    // 2. Automatically fetch and sort items from the database
    @Query(sort: \TodoItem.createdAt, order: .reverse) private var items: [TodoItem]
    
    @State private var newTitle = ""
    
    var body: some View {
        NavigationStack {
            List {
                // Add New Item Section
                HStack {
                    TextField("New task...", text: $newTitle)
                    Button("Add") {
                        addItem()
                    }
                    .disabled(newTitle.isEmpty)
                }
                
                // Display Items From Database
                ForEach(items) { item in
                    HStack {
                        Text(item.title)
                        Spacer()
                        if item.isCompleted {
                            Image(systemName: "checkmark.circle.fill")
                                .foregroundColor(.green)
                        }
                    }
                }
//                .onDelete(perform: deleteItems) // Swipe-to-delete
            }
            .navigationTitle("My Database List")
        }
    }
    
//    // 3. Insert data into the database
    private func addItem() {
        let newItem = TodoItem(title: newTitle)
        modelContext.insert(newItem) // Saved to disk automatically
        newTitle = "" // Clear text field
    }
//    
//    // 4. Delete data from the database
//    private var deleteItems(at offsets: IndexSet) {
//        for index in offsets {
//            let itemToDelete = items[index]
//            modelContext.delete(itemToDelete) // Removed from disk
//        }
//    }
}
// working - END

// working - BEGINNING - ContentView
import SwiftUI
import SwiftData

struct ContentView: View {
    // 1. Get access to the database context for writing data
    @Environment(\.modelContext) private var modelContext
    
    // 2. Automatically fetch and sort items from the database
    @Query(sort: \TodoItem.createdAt, order: .reverse) private var items: [TodoItem]
    
    @State private var newTitle = ""
    
    var body: some View {
        NavigationStack {
            List {
                // Add New Item Section
                HStack {
                    TextField("New task...", text: $newTitle)
                    Button("Add") {
                        addItem()
                    }
                    .disabled(newTitle.isEmpty)
                }
                
                // Display Items From Database
                ForEach(items) { item in
                    HStack {
                        Text(item.title)
                        Spacer()
                        if item.isCompleted {
                            Image(systemName: "checkmark.circle.fill")
                                .foregroundColor(.green)
                        }
                    }
                }
                .onDelete(perform: deleteItems) // Swipe-to-delete
            }
            .navigationTitle("My Database List")
        }
    }
    
//    // 3. Insert data into the database
    private func addItem() {
        let newItem = TodoItem(title: newTitle)
        modelContext.insert(newItem) // Saved to disk automatically
        newTitle = "" // Clear text field
    }
//    
//    // 4. Delete data from the database
    private func deleteItems(at offsets: IndexSet) {
        for index in offsets {
            let itemToDelete = items[index]
            modelContext.delete(itemToDelete) // Removed from disk
        }
    }
}


//import SwiftUI
//import SwiftData
//
//
//
//struct ContentView: View {
//    @Environment(\.modelContext) private var modelContext
//    @Query private var items: [Item]
//    @State private var fullText: String = "This is some editable text..."
//    @State private var toDoList: [String] = ["to do 1", "to do 2"]
////    let toDoList = [""]
//
//    var body: some View {
//        NavigationSplitView {
//            List {
//                ForEach(Array(toDoList.enumerated()), id: \.offset) { index, toDo in
//                                HStack {
//                                    Text("Index: \(index)") // 0, 1, 2, 3
//                                        .foregroundColor(.gray)
//                                    Spacer()
//                                    Text(toDo)
//                                }
//                            }
////                ForEach(items) { item in
////                    NavigationLink {
////                        Text("Item at \(item.timestamp)")
////                    } label: {
////                        TextEditor(text: $toDoList[toDoList.count])
////                    }
////                }
//                .onDelete { offsets in
//                    deleteItems3(at: offsets)
//                }
//            }
//            .toolbar {
//                ToolbarItem(placement: .navigationBarTrailing) {
//                    EditButton()
//                }
//                ToolbarItem {
//                    Button(action: addItem2) {
//                        Label("Add Item", systemImage: "plus")
//                    }
//                }
//            }
//        } detail: {
//            Text("Select an item")
//        }
//    }
//
//    private func addItem() {
//        withAnimation {
//            let newItem = Item(timestamp: Date(), toDo: "string")
//            modelContext.insert(newItem)
//        }
//    }
//    private func addItem2() {
//        withAnimation {
//            toDoList.append("to do 3")
//        }
//    }
//
//    private func deleteItems(offsets: IndexSet) {
//        withAnimation {
//            for index in offsets {
//                modelContext.delete(items[index])
//            }
//        }
//    }
//    private func deleteItems2(int index: Int) {
//        withAnimation {
//            toDoList.remove(at: index)
//        }
//    }
//    private func deleteItems3(at offsets: IndexSet) {
//            toDoList.remove(atOffsets: offsets)
//    }
//}
//
//#Preview {
//    ContentView()
//        .modelContainer(for: Item.self, inMemory: true)
//}

// working - END

// working - BEGINNING - to_doApp

import SwiftUI
import SwiftData

@main
struct TodoApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
        // Tell SwiftData to manage the database container for your models
        .modelContainer(for: TodoItem.self)
    }
}

// working - END

// working - beginning - TodoItem

import Foundation
import SwiftData

@Model
class TodoItem {
    var title: String
    var isCompleted: Bool
    var createdAt: Date
    
    init(title: String, isCompleted: Bool = false, createdAt: Date = .now) {
        self.title = title
        self.isCompleted = isCompleted
        self.createdAt = createdAt
    }
}

// working - END

// working - BEGINNING - to do items add to list and can be toggled with green checkmark

import SwiftUI
import SwiftData

struct ContentView: View {
    // 1. Get access to the database context for writing data
    @Environment(\.modelContext) private var modelContext
    
    // 2. Automatically fetch and sort items from the database
    @Query(sort: \TodoItem.createdAt, order: .reverse) private var items: [TodoItem]
    
    @State private var newTitle = ""
    
    var body: some View {
        NavigationStack {
            List {
                // Add New Item Section
                HStack {
                    TextField("to do item...", text: $newTitle)
                        .textInputAutocapitalization(.never) 
                    Button("Add") {
                        addItem()
                    }
                    .disabled(newTitle.isEmpty)
                }
                
                // Display Items From Database
                ForEach(items) { item in
                    HStack {
//                        Spacer()
                        Button("") {
                            item.isCompleted.toggle()
                        }
                        if item.isCompleted == false {
                            Image(systemName: "circle.fill")
                                .foregroundColor(.white)
                                .strikethrough(item.isCompleted)
                                .foregroundColor(item.isCompleted ? .green : .primary)
                                .overlay(
                                    Circle()
                                        .stroke(.gray, lineWidth: 1) // Border color and thickness
                                )
                        }
                        if item.isCompleted == true {
                            Image(systemName: "checkmark.circle.fill")
                                .foregroundColor(.green)
                                .strikethrough(item.isCompleted)
                                .foregroundColor(item.isCompleted ? .green : .primary)
                        }
                        Text(item.title)
                    }
                }
                .onDelete(perform: deleteItems) // Swipe-to-delete
            }
            .navigationTitle("to do")
        }
    }
    
//    // 3. Insert data into the database
    private func addItem() {
        let newItem = TodoItem(title: newTitle)
        modelContext.insert(newItem) // Saved to disk automatically
        newTitle = "" // Clear text field
    }
//    
//    // 4. Delete data from the database
    private func deleteItems(at offsets: IndexSet) {
        for index in offsets {
            let itemToDelete = items[index]
            modelContext.delete(itemToDelete) // Removed from disk
        }
    }
    
//    private func toggleCompletion(for item: TodoItem {
//        item.isCompleted.toggle()
//    }
}


//import SwiftUI
//import SwiftData
//
//
//
//struct ContentView: View {
//    @Environment(\.modelContext) private var modelContext
//    @Query private var items: [Item]
//    @State private var fullText: String = "This is some editable text..."
//    @State private var toDoList: [String] = ["to do 1", "to do 2"]
////    let toDoList = [""]
//
//    var body: some View {
//        NavigationSplitView {
//            List {
//                ForEach(Array(toDoList.enumerated()), id: \.offset) { index, toDo in
//                                HStack {
//                                    Text("Index: \(index)") // 0, 1, 2, 3
//                                        .foregroundColor(.gray)
//                                    Spacer()
//                                    Text(toDo)
//                                }
//                            }
////                ForEach(items) { item in
////                    NavigationLink {
////                        Text("Item at \(item.timestamp)")
////                    } label: {
////                        TextEditor(text: $toDoList[toDoList.count])
////                    }
////                }
//                .onDelete { offsets in
//                    deleteItems3(at: offsets)
//                }
//            }
//            .toolbar {
//                ToolbarItem(placement: .navigationBarTrailing) {
//                    EditButton()
//                }
//                ToolbarItem {
//                    Button(action: addItem2) {
//                        Label("Add Item", systemImage: "plus")
//                    }
//                }
//            }
//        } detail: {
//            Text("Select an item")
//        }
//    }
//
//    private func addItem() {
//        withAnimation {
//            let newItem = Item(timestamp: Date(), toDo: "string")
//            modelContext.insert(newItem)
//        }
//    }
//    private func addItem2() {
//        withAnimation {
//            toDoList.append("to do 3")
//        }
//    }
//
//    private func deleteItems(offsets: IndexSet) {
//        withAnimation {
//            for index in offsets {
//                modelContext.delete(items[index])
//            }
//        }
//    }
//    private func deleteItems2(int index: Int) {
//        withAnimation {
//            toDoList.remove(at: index)
//        }
//    }
//    private func deleteItems3(at offsets: IndexSet) {
//            toDoList.remove(atOffsets: offsets)
//    }
//}
//
//#Preview {
//    ContentView()
//        .modelContainer(for: Item.self, inMemory: true)
//}

// working - END

// working - to_doApp

import SwiftUI
import SwiftData

@main
struct TodoApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
        // Tell SwiftData to manage the database container for your models
        .modelContainer(for: TodoItem.self)
    }
}

// working - to_doApp - END

// working - TodoItem

import Foundation
import SwiftData

@Model
class TodoItem {
    var title: String
    var isCompleted: Bool
    var createdAt: Date
    
    init(title: String, isCompleted: Bool = false, createdAt: Date = .now) {
        self.title = title
        self.isCompleted = isCompleted
        self.createdAt = createdAt
    }
}

// working - TodoItem

// working - BEGINNING - gray design

import SwiftUI
import SwiftData

struct ContentView: View {
    // 1. Get access to the database context for writing data
    @Environment(\.modelContext) private var modelContext
    
    // 2. Automatically fetch and sort items from the database
    @Query(sort: \TodoItem.createdAt, order: .reverse) private var items: [TodoItem]
    
    @State private var newTitle = ""
    
    var body: some View {
        NavigationStack {
            List {
                // Add New Item Section
                HStack {
                    TextField("to do item...", text: $newTitle)
                        .textInputAutocapitalization(.never) 
                    Button("Add") {
                        addItem()
                    }
                    .disabled(newTitle.isEmpty)
                }
                
                // Display Items From Database
                ForEach(items) { item in
                    HStack {
//                        Spacer()
                        Button("") {
                            item.isCompleted.toggle()
                        }
                        if item.isCompleted == false {
                            Image(systemName: "circle.fill")
                                .foregroundColor(.white)
                                .strikethrough(item.isCompleted)
                                .foregroundColor(item.isCompleted ? .green : .primary)
                                .overlay(
                                    Circle()
                                        .stroke(.gray, lineWidth: 1) // Border color and thickness
                                )
                        }
                        if item.isCompleted == true {
                            Image(systemName: "checkmark.circle.fill")
                                .foregroundColor(.gray)
                                .strikethrough(item.isCompleted)
                                .foregroundColor(item.isCompleted ? .gray : .primary)
                        }
                        Text(item.title)
                    }
                }
                .onDelete(perform: deleteItems) // Swipe-to-delete
            }
            .navigationTitle("to do")
        }
    }
    
//    // 3. Insert data into the database
    private func addItem() {
        let newItem = TodoItem(title: newTitle)
        modelContext.insert(newItem) // Saved to disk automatically
        newTitle = "" // Clear text field
    }
//    
//    // 4. Delete data from the database
    private func deleteItems(at offsets: IndexSet) {
        for index in offsets {
            let itemToDelete = items[index]
            modelContext.delete(itemToDelete) // Removed from disk
        }
    }
    
//    private func toggleCompletion(for item: TodoItem {
//        item.isCompleted.toggle()
//    }
}


//import SwiftUI
//import SwiftData
//
//
//
//struct ContentView: View {
//    @Environment(\.modelContext) private var modelContext
//    @Query private var items: [Item]
//    @State private var fullText: String = "This is some editable text..."
//    @State private var toDoList: [String] = ["to do 1", "to do 2"]
////    let toDoList = [""]
//
//    var body: some View {
//        NavigationSplitView {
//            List {
//                ForEach(Array(toDoList.enumerated()), id: \.offset) { index, toDo in
//                                HStack {
//                                    Text("Index: \(index)") // 0, 1, 2, 3
//                                        .foregroundColor(.gray)
//                                    Spacer()
//                                    Text(toDo)
//                                }
//                            }
////                ForEach(items) { item in
////                    NavigationLink {
////                        Text("Item at \(item.timestamp)")
////                    } label: {
////                        TextEditor(text: $toDoList[toDoList.count])
////                    }
////                }
//                .onDelete { offsets in
//                    deleteItems3(at: offsets)
//                }
//            }
//            .toolbar {
//                ToolbarItem(placement: .navigationBarTrailing) {
//                    EditButton()
//                }
//                ToolbarItem {
//                    Button(action: addItem2) {
//                        Label("Add Item", systemImage: "plus")
//                    }
//                }
//            }
//        } detail: {
//            Text("Select an item")
//        }
//    }
//
//    private func addItem() {
//        withAnimation {
//            let newItem = Item(timestamp: Date(), toDo: "string")
//            modelContext.insert(newItem)
//        }
//    }
//    private func addItem2() {
//        withAnimation {
//            toDoList.append("to do 3")
//        }
//    }
//
//    private func deleteItems(offsets: IndexSet) {
//        withAnimation {
//            for index in offsets {
//                modelContext.delete(items[index])
//            }
//        }
//    }
//    private func deleteItems2(int index: Int) {
//        withAnimation {
//            toDoList.remove(at: index)
//        }
//    }
//    private func deleteItems3(at offsets: IndexSet) {
//            toDoList.remove(atOffsets: offsets)
//    }
//}
//
//#Preview {
//    ContentView()
//        .modelContainer(for: Item.self, inMemory: true)
//}


// working - END

// workign - BEGINNING - notCompleted at the top and completed at the bottom

import SwiftUI
import SwiftData

struct ContentView: View {
    // 1. Get access to the database context for writing data
    @Environment(\.modelContext) private var modelContext
    
    // 2. Automatically fetch and sort items from the database
    @Query(sort: \TodoItem.createdAt, order: .reverse) private var items: [TodoItem]
    
    @Query(filter: #Predicate<TodoItem> { item in
        item.isCompleted == false
    }) private var notCompletedItems: [TodoItem]
    
    @Query(filter: #Predicate<TodoItem> { item in
        item.isCompleted == true
    }) private var completedItems: [TodoItem]
    
    @State private var newTitle = ""
    
    var body: some View {
        NavigationStack {
            List {
                // Add New Item Section
                HStack {
                    TextField("to do item...", text: $newTitle)
                        .textInputAutocapitalization(.never) 
                    Button("Add") {
                        addItem()
                    }
                    .disabled(newTitle.isEmpty)
                }
                
                // Display Items From Database
                ForEach(notCompletedItems) { item in
                    HStack {
//                        Spacer()
                        Button("") {
                            item.isCompleted.toggle()
                        }
                        
                        if item.isCompleted == false {
                            Image(systemName: "circle.fill")
                                .foregroundColor(.white)
                                .strikethrough(item.isCompleted)
                                .foregroundColor(item.isCompleted ? .green : .primary)
                                .overlay(
                                    Circle()
                                        .stroke(.gray, lineWidth: 1) // Border color and thickness
                                )
                        }
                        if item.isCompleted == true {
                            Image(systemName: "checkmark.circle.fill")
                                .foregroundColor(.gray)
                                .strikethrough(item.isCompleted)
                                .foregroundColor(item.isCompleted ? .gray : .primary)
                        }
                        Text(item.title)
                    }
                }
                .onDelete(perform: deleteItems) // Swipe-to-delete
                ForEach(completedItems) { item in
                    HStack {
                        //                        Spacer()
                        Button("") {
                            item.isCompleted.toggle()
                        }
                        if item.isCompleted == false {
                            Image(systemName: "circle.fill")
                                .foregroundColor(.white)
                                .strikethrough(item.isCompleted)
                                .foregroundColor(item.isCompleted ? .green : .primary)
                                .overlay(
                                    Circle()
                                        .stroke(.gray, lineWidth: 1) // Border color and thickness
                                )
                        }
                        if item.isCompleted == true {
                            Image(systemName: "checkmark.circle.fill")
                                .foregroundColor(.gray)
                                .strikethrough(item.isCompleted)
                                .foregroundColor(item.isCompleted ? .gray : .primary)
                        }
                        Text(item.title)
                    }
                }
            }
            .navigationTitle("to do")
        }
    }
    
//    // 3. Insert data into the database
    private func addItem() {
        let newItem = TodoItem(title: newTitle)
        modelContext.insert(newItem) // Saved to disk automatically
        newTitle = "" // Clear text field
    }
//    
//    // 4. Delete data from the database
    private func deleteItems(at offsets: IndexSet) {
        for index in offsets {
            let itemToDelete = items[index]
            modelContext.delete(itemToDelete) // Removed from disk
        }
    }
    private func sendCompletedToBottom() {
        // deleteItems()
        print ("send completed to bottom")
//        for index in offsets {
//            let itemToDelete = items[index]
//            modelContext.delete(itemToDelete)
//        }
    }
//    private func toggleCompletion(for item: TodoItem {
//        item.isCompleted.toggle()
//    }
}


//import SwiftUI
//import SwiftData
//
//
//
//struct ContentView: View {
//    @Environment(\.modelContext) private var modelContext
//    @Query private var items: [Item]
//    @State private var fullText: String = "This is some editable text..."
//    @State private var toDoList: [String] = ["to do 1", "to do 2"]
////    let toDoList = [""]
//
//    var body: some View {
//        NavigationSplitView {
//            List {
//                ForEach(Array(toDoList.enumerated()), id: \.offset) { index, toDo in
//                                HStack {
//                                    Text("Index: \(index)") // 0, 1, 2, 3
//                                        .foregroundColor(.gray)
//                                    Spacer()
//                                    Text(toDo)
//                                }
//                            }
////                ForEach(items) { item in
////                    NavigationLink {
////                        Text("Item at \(item.timestamp)")
////                    } label: {
////                        TextEditor(text: $toDoList[toDoList.count])
////                    }
////                }
//                .onDelete { offsets in
//                    deleteItems3(at: offsets)
//                }
//            }
//            .toolbar {
//                ToolbarItem(placement: .navigationBarTrailing) {
//                    EditButton()
//                }
//                ToolbarItem {
//                    Button(action: addItem2) {
//                        Label("Add Item", systemImage: "plus")
//                    }
//                }
//            }
//        } detail: {
//            Text("Select an item")
//        }
//    }
//
//    private func addItem() {
//        withAnimation {
//            let newItem = Item(timestamp: Date(), toDo: "string")
//            modelContext.insert(newItem)
//        }
//    }
//    private func addItem2() {
//        withAnimation {
//            toDoList.append("to do 3")
//        }
//    }
//
//    private func deleteItems(offsets: IndexSet) {
//        withAnimation {
//            for index in offsets {
//                modelContext.delete(items[index])
//            }
//        }
//    }
//    private func deleteItems2(int index: Int) {
//        withAnimation {
//            toDoList.remove(at: index)
//        }
//    }
//    private func deleteItems3(at offsets: IndexSet) {
//            toDoList.remove(atOffsets: offsets)
//    }
//}
//
//#Preview {
//    ContentView()
//        .modelContainer(for: Item.self, inMemory: true)
//}

// working - END 

// working - Beginning - toggles correctly, adds correctly, has nice title

import SwiftUI
import SwiftData

struct ContentView: View {
    // 1. Get access to the database context for writing data
    @Environment(\.modelContext) private var modelContext
    
    // 2. Automatically fetch and sort items from the database
    @Query(sort: \TodoItem.createdAt, order: .reverse) private var items: [TodoItem]
        
    @State private var newTitle = ""
    
    var body: some View {
        NavigationStack {
            List {
                // Add New Item Section
                Section(header: Text("things to do")
                    .bold()
                    .font(.system(size: 24))
                    .padding(.top, 10)
                    .padding(.bottom, -32)) {
                    
                }
                HStack {
                    TextField("to do item...", text: $newTitle)
                        .textInputAutocapitalization(.never)
                    Button("Add") {
                        addItem()
                    }
                    .disabled(newTitle.isEmpty)
                }
                Section(header: Text("To Do")) {
                    ForEach(items.filter { !$0.isCompleted}) { item in
                        HStack {
                            Button("") {
                                item.isCompleted.toggle()
                            }
                            Image(systemName: "circle.fill")
                                .foregroundColor(.white)
                                .strikethrough(item.isCompleted)
                                .foregroundColor(item.isCompleted ? .green : .primary)
                                .overlay(
                                    Circle()
                                        .stroke(.gray, lineWidth: 1) // Border color and thickness
                                )
                            Text(item.title)
                        }
                    }
                    .onDelete(perform: deleteItems) // Swipe-to-delete
                }
                Section(header: Text("Completed")) {
                    ForEach(items.filter { $0.isCompleted}) { item in
                        HStack{
                            Button("") {
                                item.isCompleted.toggle()
                            }
                            Image(systemName: "checkmark.circle.fill")
                                .foregroundColor(.gray)
                                .strikethrough(item.isCompleted)
                                .foregroundColor(item.isCompleted ? .gray : .primary)
                            Text(item.title)
                        }
                    }
                    .onDelete(perform: deleteItems) // Swipe-to-delete
                }
            }
        }
        .navigationTitle("To Do")
    }
    // 3. Insert data into the database
    private func addItem() {
        let newItem = TodoItem(title: newTitle)
        modelContext.insert(newItem) // Saved to disk automatically
        newTitle = "" // Clear text field
    }
    // 4. Delete data from the database
    private func deleteItems(at offsets: IndexSet) {
        for index in offsets {
            let itemToDelete = items[index]
            modelContext.delete(itemToDelete) // Removed from disk
        }
    }
}

// working - END 

// working - BEGINNING - LoginView - working log in

//
//  LoginView.swift
//  questions
//
//  Created by Ingrid Zippe on 6/26/26.
//

import SwiftUI
import SwiftData

struct LoginView: View {
    // 1. Fetch the database context manager from the environment
    @Environment(\.modelContext) private var modelContext
    
    // 1. Inputs and interactive states
    @State private var email = ""
    @State private var password = ""
    @State private var isLoading = false
    @State private var errorMessage: String? = nil
    @State private var loginMessage = ""
    
    // 💡 Must share the exact same key name string as Step 1
    @AppStorage("isLoggedIn") private var isLoggedIn = false
    
    @State private var navigateToSignUp = false
    @State private var navigateToMainAppTabsView = false
    
    @Query private var profiles: [Profile] 
    
    // 2. Track form validity reactively
    private var isFormInvalid: Bool {
        email.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty || password.count < 6
    }
    
    var body: some View {
        NavigationStack {
            Button("Debug Object") {
                if let firstUser = profiles.first {
                    print(firstUser)
                    // Console Output: UserModel(username: 'AlexApple', email: 'alex@test.com')
                }
            }
            VStack(spacing: 24) {
                List(profiles) { profile in
                    Text(profile.email)
                }
                Spacer()
                
                // Header Brand Section
                VStack(spacing: 8) {
                    Image(systemName: "lock.shield.fill")
                        .font(.system(size: 64))
                        .foregroundColor(.blue)
                    
                    Text("Welcome Back")
                        .font(.largeTitle)
                        .bold()
                }
                .padding(.bottom, 16)
                
                // Input Fields Section
                VStack(spacing: 16) {
                    // Email Field
                    HStack {
                        Image(systemName: "envelope")
                            .foregroundColor(.gray)
                        TextField("Email Address", text: $email)
                            .keyboardType(.emailAddress)
                            .textInputAutocapitalization(.never) // 💡 Shifts keyboard off
                            .autocorrectionDisabled(true)
                            .textContentType(.emailAddress)   // 💡 Auto-suggests saved emails
                    }
                    .padding()
                    .background(Color(.systemGray6))
                    .cornerRadius(12)
                    
                    // Password Field
                    HStack {
                        Image(systemName: "lock")
                            .foregroundColor(.gray)
                        SecureField("Password", text: $password) // 💡 Masks typed text
                            .textContentType(.password)         // 💡 Auto-fills from iCloud Keychain
                    }
                    .padding()
                    .background(Color(.systemGray6))
                    .cornerRadius(12)
                }
                
                // Forgot Password Link
                HStack {
                    Spacer()
                    Button("Forgot Password?") {
                        // Action handle
                    }
                    .font(.footnote)
                    .foregroundColor(.blue)
                }
                
                // Error feedback display
                if let errorMessage {
                    Text(errorMessage)
                        .foregroundColor(.red)
                        .font(.caption)
                        .multilineTextAlignment(.center)
                }
                Section {
                    Button("Log In", action: verifyUserCredentials)
                        .frame(maxWidth: .infinity, alignment: .center)
                }
                .navigationDestination(isPresented: $navigateToMainAppTabsView) {
                    MainAppTabsView() // The screen view you want to push to [1]
                }
                
                if !loginMessage.isEmpty {
                    Text(loginMessage)
                        .foregroundColor(.secondary)
                    // navigateToSignUp = true
                    // MainAppTabsView()
                }
                
                // Action Submission Button
                Button {
                    handleLoginAction()
                    print("button clicked")
                    loginByQueryingDatabase()
                } label: {
                    HStack {
                        if isLoading {
                            ProgressView()
                                .tint(.white)
                        } else {
                            Text("Sign In")
                                .bold()
                        }
                    }
                    .frame(maxWidth: .infinity)
                    .padding()
                    .background(isFormInvalid ? Color.blue : Color.gray)
                    .foregroundColor(.white)
                    .cornerRadius(12)
                }
                .disabled(isFormInvalid || isLoading) // 💡 Prevents junk API execution
                
                Spacer()
                
                // Footer / Alternative Sign-up Link
                // 2. Your standard SwiftUI button
                Button {
                    // Perform background actions or calculations here first
                    print("Button tapped, saving data...")
                    
                    // 3. Flip the state variable to trigger the screen switch
                    navigateToSignUp = true
                } label: {
                    HStack(spacing: 4) {
                        Text("Don't have an account?")
                            .foregroundColor(.gray)
                        Text("Sign Up 2")
                            .bold()
                            .foregroundColor(.blue)
                    }
                    .font(.subheadline)
                }
                .padding(.horizontal)
                // 4. Attach the destination listener inside the NavigationStack [1]
                .navigationDestination(isPresented: $navigateToSignUp) {
                    SignUpView() // The screen view you want to push to [1]
                }
            }
            .padding(.horizontal, 24)
        }
    }
    
    private func checkUserData(profile: Profile) {
        // 💡 Explicitly target the individual database attributes
        print("👤 Email: \(profile.email) | Password: \(profile.password)")
    }
    
    func verifyUserCredentials() {
            // Clean the input to prevent matching trailing spaces or accidental uppercase inputs
            let optimizedEmail = email.trimmingCharacters(in: .whitespacesAndNewlines).lowercased()
            let typedPassword = password
            
            // 1. Build a localized query filter matching the specific credentials
            let loginDescriptor = FetchDescriptor<Profile>(
                predicate: #Predicate<Profile> { profile in
                    profile.email == optimizedEmail && profile.password == typedPassword
                }
            )
            
            // 2. Limit the fetch evaluation to 1 record to save system resources
            var limitedDescriptor = loginDescriptor
            limitedDescriptor.fetchLimit = 1
            
            do {
                // 3. Look through the database
                let matchingUsers = try modelContext.fetch(limitedDescriptor)
                
                // 4. Check if a user record was returned
                if let authenticatedUser = matchingUsers.first {
                    loginMessage = "Welcome back, \(authenticatedUser.email)!"
                    navigateToMainAppTabsView = true
                    // Proceed with your app navigation/session flow here
                } else {
                    loginMessage = "Access denied. Invalid email or password."
                }
            } catch {
                loginMessage = "Database error: \(error.localizedDescription)"
            }
        }
    
    // 3. Mock authentication workflow execution
    private func handleLoginAction() {
        isLoading = true
        errorMessage = nil
        
        // Simulating network authentication latency
        DispatchQueue.main.asyncAfter(deadline: .now() + 1.5) {
            isLoading = false
            
            if email == "hello@test.com" && password == "password123" {
                print("Login Success!")
                // Route user to target core App Dashboard view container
                withAnimation {
                    isLoggedIn = true
                }
            } else {
                errorMessage = "Invalid email identity or mismatching password record."
            }
        }
    }
    
    private func loginByQueryingDatabase() {
        print("loginByQueryingDatabase")
        isLoading = true
        errorMessage = nil
        
//        let email = emailInput.lowercased().trimmingCharacters(in: .whitespacesAndNewlines)
//        let password = passwordInput // Match exact casing/hashes
        
        // Simulating network authentication latency
        DispatchQueue.main.asyncAfter(deadline: .now() + 1.5) {
            isLoading = false
            
            // Define the search criteria
            let descriptor = FetchDescriptor<Profile>(
                predicate: #Predicate { profile in
                    profile.email == email && profile.password == password
                }
            )
            do {
                // Execute the query
                print("matching users")
                let matchedUsers = try self.modelContext.fetch(descriptor)
                
                if let loggedInUser = matchedUsers.first {
                    print("Login successful! Welcome \(loggedInUser.email)")
                    withAnimation {
                        isLoggedIn = true
                    }
                } else {
                    print("Invalid email or password.")
                }
            } catch {
                print("Fetch failed: \(error.localizedDescription)")
            }
            
//            if email == queried email && password == querie password {
//                print("Login Success!")
//                // Route user to target core App Dashboard view container
//                withAnimation {
//                    isLoggedIn = true
//                }
//            } else {
//                errorMessage = "Invalid email identity or mismatching password record."
//            }
        }
        
        
        
    }
}

#Preview {
    LoginView()
}


// working - END

// working - BEGINNING - login working - tabs working 

// LoginView - B

//
//  LoginView.swift
//  questions
//
//  Created by Ingrid Zippe on 6/26/26.
//

import SwiftUI
import SwiftData

struct LoginView: View {
    // 1. Fetch the database context manager from the environment
    @Environment(\.modelContext) private var modelContext
    
    // 1. Inputs and interactive states
    @State private var email = ""
    @State private var password = ""
    @State private var isLoading = false
    @State private var errorMessage: String? = nil
    @State private var loginMessage = ""
    
    // 💡 Must share the exact same key name string as Step 1
    @AppStorage("isLoggedIn") private var isLoggedIn = false
    @AppStorage("loggedInUserEmail") private var loggedInUserEmail: String = ""
    
    @State private var navigateToSignUp = false
    @State private var navigateToMainAppTabsView = false
    
    @Query private var profiles: [Profile] 
    
    // 2. Track form validity reactively
    private var isFormInvalid: Bool {
        email.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty || password.count < 6
    }
    
    var body: some View {
        NavigationStack {
            Button("Debug Object") {
                if let firstUser = profiles.first {
                    print(firstUser)
                    // Console Output: UserModel(username: 'AlexApple', email: 'alex@test.com')
                }
            }
            VStack(spacing: 24) {
                List(profiles) { profile in
                    Text(profile.email)
                }
                Spacer()
                
                // Header Brand Section
                VStack(spacing: 8) {
                    Image(systemName: "lock.shield.fill")
                        .font(.system(size: 64))
                        .foregroundColor(.blue)
                    
                    Text("Welcome Back")
                        .font(.largeTitle)
                        .bold()
                }
                .padding(.bottom, 16)
                
                // Input Fields Section
                VStack(spacing: 16) {
                    // Email Field
                    HStack {
                        Image(systemName: "envelope")
                            .foregroundColor(.gray)
                        TextField("Email Address", text: $email)
                            .keyboardType(.emailAddress)
                            .textInputAutocapitalization(.never) // 💡 Shifts keyboard off
                            .autocorrectionDisabled(true)
                            .textContentType(.emailAddress)   // 💡 Auto-suggests saved emails
                    }
                    .padding()
                    .background(Color(.systemGray6))
                    .cornerRadius(12)
                    
                    // Password Field
                    HStack {
                        Image(systemName: "lock")
                            .foregroundColor(.gray)
                        SecureField("Password", text: $password) // 💡 Masks typed text
                            .textContentType(.password)         // 💡 Auto-fills from iCloud Keychain
                    }
                    .padding()
                    .background(Color(.systemGray6))
                    .cornerRadius(12)
                }
                
                // Forgot Password Link
                HStack {
                    Spacer()
                    Button("Forgot Password?") {
                        // Action handle
                    }
                    .font(.footnote)
                    .foregroundColor(.blue)
                }
                
                // Error feedback display
                if let errorMessage {
                    Text(errorMessage)
                        .foregroundColor(.red)
                        .font(.caption)
                        .multilineTextAlignment(.center)
                }
                Section {
                    Button("Log In", action: verifyUserCredentials)
                        .frame(maxWidth: .infinity, alignment: .center)
                }
//                .navigationDestination(isPresented: $navigateToMainAppTabsView) {
//                    MainAppTabsView() // The screen view you want to push to [1]
//                }
                .navigationDestination(for: Profile.self) { selectedProfile in
                    MainAppTabsView()
                }
                
                if !loginMessage.isEmpty {
                    Text(loginMessage)
                        .foregroundColor(.secondary)
                    // navigateToSignUp = true
                    // MainAppTabsView()
                }
                
                // Action Submission Button
                Button {
                    handleLoginAction()
                    print("button clicked")
                    loginByQueryingDatabase()
                } label: {
                    HStack {
                        if isLoading {
                            ProgressView()
                                .tint(.white)
                        } else {
                            Text("Sign In")
                                .bold()
                        }
                    }
                    .frame(maxWidth: .infinity)
                    .padding()
                    .background(isFormInvalid ? Color.blue : Color.gray)
                    .foregroundColor(.white)
                    .cornerRadius(12)
                }
                .disabled(isFormInvalid || isLoading) // 💡 Prevents junk API execution
                
                Spacer()
                
                // Footer / Alternative Sign-up Link
                // 2. Your standard SwiftUI button
                Button {
                    // Perform background actions or calculations here first
                    print("Button tapped, saving data...")
                    
                    // 3. Flip the state variable to trigger the screen switch
                    navigateToSignUp = true
                } label: {
                    HStack(spacing: 4) {
                        Text("Don't have an account?")
                            .foregroundColor(.gray)
                        Text("Sign Up 2")
                            .bold()
                            .foregroundColor(.blue)
                    }
                    .font(.subheadline)
                }
                .padding(.horizontal)
                // 4. Attach the destination listener inside the NavigationStack [1]
                .navigationDestination(isPresented: $navigateToSignUp) {
                    SignUpView() // The screen view you want to push to [1]
                }
            }
            .padding(.horizontal, 24)
        }
    }
    
    private func checkUserData(profile: Profile) {
        // 💡 Explicitly target the individual database attributes
        print("👤 Email: \(profile.email) | Password: \(profile.password)")
    }
    
    func verifyUserCredentials() {
            // Clean the input to prevent matching trailing spaces or accidental uppercase inputs
            let optimizedEmail = email.trimmingCharacters(in: .whitespacesAndNewlines).lowercased()
            let typedPassword = password
            
            // 1. Build a localized query filter matching the specific credentials
            let loginDescriptor = FetchDescriptor<Profile>(
                predicate: #Predicate<Profile> { profile in
                    profile.email == optimizedEmail && profile.password == typedPassword
                }
            )
            
            // 2. Limit the fetch evaluation to 1 record to save system resources
            var limitedDescriptor = loginDescriptor
            limitedDescriptor.fetchLimit = 1
            
            do {
                // 3. Look through the database
                let matchingUsers = try modelContext.fetch(limitedDescriptor)
                
                // 4. Check if a user record was returned
                if let authenticatedUser = matchingUsers.first {
                    loginMessage = "Welcome back, \(authenticatedUser.email)!"
                    // here
                    self.loggedInUserEmail = authenticatedUser.email
                    // @AppStorage("userEmail") var userEmail: String?

                    navigateToMainAppTabsView = true
                    // Proceed with your app navigation/session flow here
                } else {
                    loginMessage = "Access denied. Invalid email or password."
                }
            } catch {
                loginMessage = "Database error: \(error.localizedDescription)"
            }
        }
    
    // 3. Mock authentication workflow execution
    private func handleLoginAction() {
        isLoading = true
        errorMessage = nil
        
        // Simulating network authentication latency
        DispatchQueue.main.asyncAfter(deadline: .now() + 1.5) {
            isLoading = false
            
            if email == "hello@test.com" && password == "password123" {
                print("Login Success!")
                // Route user to target core App Dashboard view container
                withAnimation {
                    isLoggedIn = true
                }
            } else {
                errorMessage = "Invalid email identity or mismatching password record."
            }
        }
    }
    
    private func loginByQueryingDatabase() {
        print("loginByQueryingDatabase")
        isLoading = true
        errorMessage = nil
        
//        let email = emailInput.lowercased().trimmingCharacters(in: .whitespacesAndNewlines)
//        let password = passwordInput // Match exact casing/hashes
        
        // Simulating network authentication latency
        DispatchQueue.main.asyncAfter(deadline: .now() + 1.5) {
            isLoading = false
            
            // Define the search criteria
            let descriptor = FetchDescriptor<Profile>(
                predicate: #Predicate { profile in
                    profile.email == email && profile.password == password
                }
            )
            do {
                // Execute the query
                print("matching users")
                let matchedUsers = try self.modelContext.fetch(descriptor)
                
                if let loggedInUser = matchedUsers.first {
                    print("Login successful! Welcome \(loggedInUser.email)")
                    withAnimation {
                        isLoggedIn = true
                    }
                } else {
                    print("Invalid email or password.")
                }
            } catch {
                print("Fetch failed: \(error.localizedDescription)")
            }
            
//            if email == queried email && password == querie password {
//                print("Login Success!")
//                // Route user to target core App Dashboard view container
//                withAnimation {
//                    isLoggedIn = true
//                }
//            } else {
//                errorMessage = "Invalid email identity or mismatching password record."
//            }
        }
        
        
        
    }
}

#Preview {
    LoginView()
}

// LoginView - E 

// MainAppTabsView - B

import SwiftUI
import SwiftData

struct MainAppTabsView: View {
    // 1. Get access to the database context for writing data
    @Environment(\.modelContext) private var modelContext
    
    // 2. Automatically fetch and sort items from the database
    @Query(sort: \TodoItem.createdAt, order: .reverse) private var items: [TodoItem]
        
    @State private var newTitle = ""
    
    @State private var selectedTab = 1
    
    @State private var showingSettingsSheet = false
    
    let email: String = ""
    
    var body: some View {
        TabView(selection: $selectedTab) {
            // --- TAB 1 ---
            NavigationStack {
                QuestionsView()
            }
            .tabItem {
                Label("Questions", systemImage: "house.fill")
            }
            .tag(1)
            // --- TAB 2 ---
            NavigationStack {
                ProfileView()
            }
            .tabItem {
                Label("Profile", systemImage: "person.fill")
            }
            .tag(2)
            
        }
    }
    // 3. Insert data into the database
    private func addItem() {
        let newItem = TodoItem(title: newTitle)
        modelContext.insert(newItem) // Saved to disk automatically
        newTitle = "" // Clear text field
        print(items)
    }
    // 4. Delete data from the database
    private func deleteItems(at offsets: IndexSet) {
        for index in offsets {
            let itemToDelete = items[index]
            modelContext.delete(itemToDelete) // Removed from disk
        }
    }
}

// MainAppTabsView - E

// Profile - B

import Foundation
import SwiftData

@Model
class Profile {
    var id = UUID()
    var email: String
    var password: String
    var createdAt: Date
    
    init(email: String, password: String, createdAt: Date = .now) {
        self.id = UUID()
        self.email = email
        self.password = password
        self.createdAt = createdAt
    }

}

// Profile - E

// ProfileView - B

//
//  ProfileView.swift
//  questions
//
//  Created by Ingrid Zippe on 6/26/26.
//

import SwiftUI

struct ProfileView: View {
    // 💡 Read the username string directly from local device memory
    @AppStorage("username") private var savedUsername = "Guest User"
    @AppStorage("email") private var savedEmail = "No email provided"
    
    @State private var showingSettingsSheet = false
    
    // let profile: Profile
    
    var body: some View {
        NavigationStack {
            VStack(spacing: 20) {
                // Profile Avatar Frame
                Image(systemName: "person.crop.circle.fill")
                    .font(.system(size: 90))
                    .foregroundColor(.blue)
                    .padding(.top, 40)
                
                // 💡 Displaying the profile text dynamically
                VStack(spacing: 4) {
                    Text(savedUsername)
                        .font(.title)
                        .bold()
                    
                    Text(savedEmail)
                        .font(.subheadline)
                        .foregroundColor(.gray)
                }
                
                Spacer()
            }
            .navigationTitle("Profile")
            .toolbar {
                ToolbarItem(placement: .topBarTrailing) {
                    Button {
                        showingSettingsSheet = true
                    } label: {
                        Image(systemName: "gearshape")
                            .font(.title3)
                    }
                }
            }
            // Displays your settings options inside a sliding model sheet popup
            .sheet(isPresented: $showingSettingsSheet) {
                SettingsView()
            }
            .navigationTitle("My Profile")
        }
    }
}


// ProfileView - E

// questionsApp - B

//
//  questionsApp.swift
//  questions
//
//  Created by Ingrid Zippe on 6/26/26.
//

import SwiftUI
import SwiftData


@main
struct questionsApp: App {
    // 1. Fetch the database context manager from the environment
    @Environment(\.modelContext) private var modelContext
    
    // 1. Initialize your global SwiftData container
    let container: ModelContainer
        
    // 💡 Read the login state globally from the device storage
    @AppStorage("isLoggedIn") private var isLoggedIn = false

    // 2. Track the logged-in user email using @AppStorage (Persisted in UserDefaults)
    @AppStorage("loggedInUserEmail") private var loggedInUserEmail: String = ""
    
    init() {
        do {
            container = try ModelContainer(for: Profile.self)
        } catch {
            fatalError("Failed to initialize ModelContainer: \(error.localizedDescription)")
        }
    }
        
    //    var sharedModelContainer: ModelContainer = {
    //        let schema = Schema([
    //            Item.self,
    //        ])
    //        let modelConfiguration = ModelConfiguration(schema: schema, isStoredInMemoryOnly: false)
    //
    //        do {
    //            return try ModelContainer(for: schema, configurations: [modelConfiguration])
    //        } catch {
    //            fatalError("Could not create ModelContainer: \(error)")
    //        }
    //    }()
        
    var body: some Scene {
        WindowGroup {
            // 3. Decide which view tree to render based on session state
            if loggedInUserEmail.isEmpty {
                LoginView()
            } else {
                // Pass the identifier down so the view can extract the model instance
                MainAppTabsView()
            }
//            // 💡 The view automatically swaps out when the boolean shifts
//            if isLoggedIn {
//                MainAppTabsView() // Your core application layout or tabs
//            } else {
//                LoginView()       // The security login wrapper
//            }
        }
        // .modelContainer(sharedModelContainer)
        .modelContainer(for: Profile.self)
    }
}


// questionsApp - E

// QuestionsView - B

//
//  QuestionsView.swift
//  questions
//
//  Created by Ingrid Zippe on 6/26/26.
//

import SwiftUI

struct QuestionsView: View {
    // 💡 Read the username string directly from local device memory
    @AppStorage("username") private var savedUsername = "Guest User"
    @AppStorage("email") private var savedEmail = "No email provided"
    
    var body: some View {
        NavigationStack {
            // 💡 Displaying the profile text dynamically
            VStack(spacing: 4) {
                Text(savedUsername)
                    .font(.title)
                    .bold()
                Text(savedEmail)
                    .font(.subheadline)
                    .foregroundColor(.gray)
            }
            .navigationTitle("Questions")
        }
    }
}


// QuestionsView - E

import SwiftUI

struct SettingsView: View {
    @AppStorage("isLoggedIn") private var isLoggedIn = true

    var body: some View {
        Button(role: .destructive) {
            // 💡 Instantly triggers the root switch and shows LoginView again
            withAnimation {
                isLoggedIn = false
            }
        } label: {
            Text("Log Out")
        }
    }
}



// SettingsView - B

// SignUpView - B

//
//  SignUpView.swift
//  questions
//
//  Created by Ingrid Zippe on 6/26/26.
//

import SwiftUI
import SwiftData

struct SignUpView: View {
    // 1. Fetch the database context manager from the environment
    @Environment(\.modelContext) private var modelContext
   // @Environment(\.dismiss) private var dismiss
    
    @State private var emailInput = ""
    @State private var passwordInput = ""
    
    // 1. Inputs and interactive states
    @State private var email = ""
    @State private var password = ""
    @State private var isLoading = false
    @State private var errorMessage: String? = nil
    
    // 💡 Must share the exact same key name string as Step 1
    @AppStorage("isLoggedIn") private var isLoggedIn = false
    
    @Query private var profiles: [Profile]
    
    // 2. Track form validity reactively
    private var isFormInvalid: Bool {
        email.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty || password.count < 6
    }
    
    var body: some View {
        NavigationStack {
            Form {
                Section("User Information") {
                    TextField("Email", text: $emailInput)
                        .keyboardType(.emailAddress)
                        .textInputAutocapitalization(.never)
                        .autocorrectionDisabled(true)
                    
                    TextField("Password", text: $passwordInput)
                        .textInputAutocapitalization(.never)
                        .autocorrectionDisabled(true)
                }
                
                Section {
                    Button {
                        saveNewUser()
                    } label: {
                        Text("Create User Account")
                            .frame(maxWidth: .infinity, alignment: .center)
                    }
                    // Disable the button if fields are blank
                    .disabled(emailInput.isEmpty || passwordInput.isEmpty)
                }
                Button("Print All Database Users") {
                    print("--- DATABASE EXPORT (Total: \(profiles.count)) ---")
                    for (index, profile) in profiles.enumerated() {
                        print("Index \(index): Email = \(profile.email), Password = \(profile.password)")
                    }
                    print("---------------------------------------")
                }
            }
            .navigationTitle("New User")
            .onAppear {
                print("📂 Database Location: \(URL.applicationSupportDirectory.path)")
            }
        }
    }
    
    private func addItem() {
        let newItem = Profile(email: emailInput, password: passwordInput)
        modelContext.insert(newItem) // Saved to disk automatically
        print(profiles)
    }
    
    // 2. The insertion logic function
    @MainActor
    private func saveNewUser() {
        // Create the new model instance matching the user inputs
        let newProfile = Profile(email: emailInput, password: passwordInput)
        
        // Tell SwiftData to insert the object into the database context
        modelContext.insert(newProfile)
        // try? modelContext.save() // Always call manual save on async completio
        
        // 2. 💡 FORCE IMMEDIATE SAVE TO DISK
        do {
            try modelContext.save()
            print("Successfully committed to SQLite disk file!")
        } catch {
            print("Database save failed: \(error.localizedDescription)")
        }
        
        // try? modelContext.save() // Always save manually when changing async states
        
        // 💡 Pro-Tip: SwiftData auto-saves when the main thread idles.
        // There is no mandatory requirement to call try? modelContext.save() manually!
        
        // dismiss() // Close the current screen or view drawer
    }
    
    // 3. Mock authentication workflow execution
    private func handleLoginAction() {
        isLoading = true
        errorMessage = nil
        
        // Simulating network authentication latency
        DispatchQueue.main.asyncAfter(deadline: .now() + 1.5) {
            isLoading = false
            
            if email == "hello@test.com" && password == "password123" {
                print("Login Success!")
                // Route user to target core App Dashboard view container
                withAnimation {
                    isLoggedIn = true
                }
            } else {
                errorMessage = "Invalid email identity or mismatching password record."
            }
        }
    }
}

#Preview {
    SignUpView()
}

// SignUpView - E

// SettingsView - E

// working - END 



// working - BEGINNING - Displaying User Information on Profile

// LoginView - B
//
//  LoginView.swift
//  questions
//
//  Created by Ingrid Zippe on 6/26/26.
//

import SwiftUI
import SwiftData

struct LoginView: View {
    // 1. Fetch the database context manager from the environment
    @Environment(\.modelContext) private var modelContext
    
    // 1. Inputs and interactive states
    @State private var email = ""
    @State private var password = ""
    @State private var isLoading = false
    @State private var errorMessage: String? = nil
    @State private var loginMessage = ""
    
    // 💡 Must share the exact same key name string as Step 1
    @AppStorage("isLoggedIn") private var isLoggedIn = false
    @AppStorage("loggedInUserEmail") private var loggedInUserEmail: String = ""
    
    @State private var navigateToSignUp = false
    @State private var navigateToMainAppTabsView = false
    
    @Query private var profiles: [Profile] 
    
    // 2. Track form validity reactively
    private var isFormInvalid: Bool {
        email.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty || password.count < 6
    }
    
    var body: some View {
        NavigationStack {
            Button("Debug Object") {
                if let firstUser = profiles.first {
                    print(firstUser)
                    // Console Output: UserModel(username: 'AlexApple', email: 'alex@test.com')
                }
            }
            VStack(spacing: 24) {
                List(profiles) { profile in
                    Text(profile.email)
                }
                Spacer()
                
                // Header Brand Section
                VStack(spacing: 8) {
                    Image(systemName: "lock.shield.fill")
                        .font(.system(size: 64))
                        .foregroundColor(.blue)
                    
                    Text("Welcome Back")
                        .font(.largeTitle)
                        .bold()
                }
                .padding(.bottom, 16)
                
                // Input Fields Section
                VStack(spacing: 16) {
                    // Email Field
                    HStack {
                        Image(systemName: "envelope")
                            .foregroundColor(.gray)
                        TextField("Email Address", text: $email)
                            .keyboardType(.emailAddress)
                            .textInputAutocapitalization(.never) // 💡 Shifts keyboard off
                            .autocorrectionDisabled(true)
                            .textContentType(.emailAddress)   // 💡 Auto-suggests saved emails
                    }
                    .padding()
                    .background(Color(.systemGray6))
                    .cornerRadius(12)
                    
                    // Password Field
                    HStack {
                        Image(systemName: "lock")
                            .foregroundColor(.gray)
                        SecureField("Password", text: $password) // 💡 Masks typed text
                            .textContentType(.password)         // 💡 Auto-fills from iCloud Keychain
                    }
                    .padding()
                    .background(Color(.systemGray6))
                    .cornerRadius(12)
                }
                
                // Forgot Password Link
                HStack {
                    Spacer()
                    Button("Forgot Password?") {
                        // Action handle
                    }
                    .font(.footnote)
                    .foregroundColor(.blue)
                }
                
                // Error feedback display
                if let errorMessage {
                    Text(errorMessage)
                        .foregroundColor(.red)
                        .font(.caption)
                        .multilineTextAlignment(.center)
                }
                Section {
                    Button("Log In", action: verifyUserCredentials)
                        .frame(maxWidth: .infinity, alignment: .center)
                }
//                .navigationDestination(isPresented: $navigateToMainAppTabsView) {
//                    MainAppTabsView() // The screen view you want to push to [1]
//                }
                .navigationDestination(for: Profile.self) { selectedProfile in
                    MainAppTabsView()
                }
                
                if !loginMessage.isEmpty {
                    Text(loginMessage)
                        .foregroundColor(.secondary)
                    // navigateToSignUp = true
                    // MainAppTabsView()
                }
                
                // Action Submission Button
                Button {
                    handleLoginAction()
                    print("button clicked")
                    loginByQueryingDatabase()
                } label: {
                    HStack {
                        if isLoading {
                            ProgressView()
                                .tint(.white)
                        } else {
                            Text("Sign In")
                                .bold()
                        }
                    }
                    .frame(maxWidth: .infinity)
                    .padding()
                    .background(isFormInvalid ? Color.blue : Color.gray)
                    .foregroundColor(.white)
                    .cornerRadius(12)
                }
                .disabled(isFormInvalid || isLoading) // 💡 Prevents junk API execution
                
                Spacer()
                
                // Footer / Alternative Sign-up Link
                // 2. Your standard SwiftUI button
                Button {
                    // Perform background actions or calculations here first
                    print("Button tapped, saving data...")
                    
                    // 3. Flip the state variable to trigger the screen switch
                    navigateToSignUp = true
                } label: {
                    HStack(spacing: 4) {
                        Text("Don't have an account?")
                            .foregroundColor(.gray)
                        Text("Sign Up 2")
                            .bold()
                            .foregroundColor(.blue)
                    }
                    .font(.subheadline)
                }
                .padding(.horizontal)
                // 4. Attach the destination listener inside the NavigationStack [1]
                .navigationDestination(isPresented: $navigateToSignUp) {
                    SignUpView() // The screen view you want to push to [1]
                }
            }
            .padding(.horizontal, 24)
        }
    }
    
    private func checkUserData(profile: Profile) {
        // 💡 Explicitly target the individual database attributes
        print("👤 Email: \(profile.email) | Password: \(profile.password)")
    }
    
    func verifyUserCredentials() {
            // Clean the input to prevent matching trailing spaces or accidental uppercase inputs
            let optimizedEmail = email.trimmingCharacters(in: .whitespacesAndNewlines).lowercased()
            let typedPassword = password
            
            // 1. Build a localized query filter matching the specific credentials
            let loginDescriptor = FetchDescriptor<Profile>(
                predicate: #Predicate<Profile> { profile in
                    profile.email == optimizedEmail && profile.password == typedPassword
                }
            )
            
            // 2. Limit the fetch evaluation to 1 record to save system resources
            var limitedDescriptor = loginDescriptor
            limitedDescriptor.fetchLimit = 1
            
            do {
                // 3. Look through the database
                let matchingUsers = try modelContext.fetch(limitedDescriptor)
                
                // 4. Check if a user record was returned
                if let authenticatedUser = matchingUsers.first {
                    loginMessage = "Welcome back, \(authenticatedUser.email)!"
                    // here
                    self.loggedInUserEmail = authenticatedUser.email
                    // @AppStorage("userEmail") var userEmail: String?

                    navigateToMainAppTabsView = true
                    // Proceed with your app navigation/session flow here
                } else {
                    loginMessage = "Access denied. Invalid email or password."
                }
            } catch {
                loginMessage = "Database error: \(error.localizedDescription)"
            }
        }
    
    // 3. Mock authentication workflow execution
    private func handleLoginAction() {
        isLoading = true
        errorMessage = nil
        
        // Simulating network authentication latency
        DispatchQueue.main.asyncAfter(deadline: .now() + 1.5) {
            isLoading = false
            
            if email == "hello@test.com" && password == "password123" {
                print("Login Success!")
                // Route user to target core App Dashboard view container
                withAnimation {
                    isLoggedIn = true
                }
            } else {
                errorMessage = "Invalid email identity or mismatching password record."
            }
        }
    }
    
    private func loginByQueryingDatabase() {
        print("loginByQueryingDatabase")
        isLoading = true
        errorMessage = nil
        
//        let email = emailInput.lowercased().trimmingCharacters(in: .whitespacesAndNewlines)
//        let password = passwordInput // Match exact casing/hashes
        
        // Simulating network authentication latency
        DispatchQueue.main.asyncAfter(deadline: .now() + 1.5) {
            isLoading = false
            
            // Define the search criteria
            let descriptor = FetchDescriptor<Profile>(
                predicate: #Predicate { profile in
                    profile.email == email && profile.password == password
                }
            )
            do {
                // Execute the query
                print("matching users")
                let matchedUsers = try self.modelContext.fetch(descriptor)
                
                if let loggedInUser = matchedUsers.first {
                    print("Login successful! Welcome \(loggedInUser.email)")
                    withAnimation {
                        isLoggedIn = true
                    }
                } else {
                    print("Invalid email or password.")
                }
            } catch {
                print("Fetch failed: \(error.localizedDescription)")
            }
            
//            if email == queried email && password == querie password {
//                print("Login Success!")
//                // Route user to target core App Dashboard view container
//                withAnimation {
//                    isLoggedIn = true
//                }
//            } else {
//                errorMessage = "Invalid email identity or mismatching password record."
//            }
        }
        
        
        
    }
}

#Preview {
    LoginView()
}

// LoginView - E

// MainAppTabsView - B

import SwiftUI
import SwiftData

struct MainAppTabsView: View {
    // 1. Get access to the database context for writing data
    @Environment(\.modelContext) private var modelContext
    
    // 2. Automatically fetch and sort items from the database
    @Query(sort: \TodoItem.createdAt, order: .reverse) private var items: [TodoItem]
        
    @State private var newTitle = ""
    
    @State private var selectedTab = 1
    
    @State private var showingSettingsSheet = false
    
    let email: String = ""
    
    var body: some View {
        TabView(selection: $selectedTab) {
            // --- TAB 1 ---
            NavigationStack {
                QuestionsView()
            }
            .tabItem {
                Label("Questions", systemImage: "house.fill")
            }
            .tag(1)
            // --- TAB 2 ---
            NavigationStack {
                ProfileView()
            }
            .tabItem {
                Label("Profile", systemImage: "person.fill")
            }
            .tag(2)
            
        }
    }
    // 3. Insert data into the database
    private func addItem() {
        let newItem = TodoItem(title: newTitle)
        modelContext.insert(newItem) // Saved to disk automatically
        newTitle = "" // Clear text field
        print(items)
    }
    // 4. Delete data from the database
    private func deleteItems(at offsets: IndexSet) {
        for index in offsets {
            let itemToDelete = items[index]
            modelContext.delete(itemToDelete) // Removed from disk
        }
    }
}


// MainAppTabsView - E

// Profile - B

import Foundation
import SwiftData

@Model
class Profile {
    var id = UUID()
    var email: String
    var password: String
    var createdAt: Date
    
    init(email: String, password: String, createdAt: Date = .now) {
        self.id = UUID()
        self.email = email
        self.password = password
        self.createdAt = createdAt
    }

}

// Profile - E

// ProfileView - B

//
//  ProfileView.swift
//  questions
//
//  Created by Ingrid Zippe on 6/26/26.
//

import SwiftUI
import SwiftData

struct ProfileView: View {
    // 💡 Read the username string directly from local device memory
    @AppStorage("username") private var savedUsername = "Guest User"
    @AppStorage("email") private var savedEmail = "No email provided"
    
    // 1. Pull the email string from AppStorage
    @AppStorage("loggedInUserEmail") private var loggedInUserEmail: String = ""
    
    // 2. Declare the query property (do not assign a filter here)
    @Query private var matchedUsers: [Profile]
    
    // 3. Create a custom initializer to read the AppStorage string
    init() {
        // Fetch the value directly from UserDefaults during initialization
        let savedEmail = UserDefaults.standard.string(forKey: "loggedInUserEmail") ?? ""
        let cleanEmail = savedEmail.lowercased().trimmingCharacters(in: .whitespacesAndNewlines)
        
        // Build the database search filter
        let filter = #Predicate<Profile> { profile in
            profile.email == cleanEmail
        }
        
        // Inject the filter back into the underlying @Query property wrapper
        _matchedUsers = Query(filter: filter)
    }
    
    @State private var showingSettingsSheet = false
    
    // let profile: Profile
    
    var body: some View {
        NavigationStack {
            Group {
                // 4. Safely unwrap the first matching database object
                if let currentUser = matchedUsers.first {
                    Form {
                        Section("Profile Information") {
                            Text("Email: \(currentUser.email)")
                            
                            // Editable fields automatically sync with SwiftData
                            TextField("Email", text: Bindable(currentUser).email)
                        }
                        
                        Button("Log Out", role: .destructive) {
                            // Clearing this instantly updates the UI layout state
                            loggedInUserEmail = ""
                        }
                    }
                } else {
                    // Fallback loader if the database is lagging or the user was deleted
                    ContentUnavailableView(
                        "Profile Not Found",
                        systemImage: "person.crop.circle.badge.exclamationmark",
                        description: Text("Please try logging in again.")
                    )
                }
            }
            .navigationTitle("Dashboard")
        }
        NavigationStack {
            VStack(spacing: 20) {
                // Profile Avatar Frame
                Image(systemName: "person.crop.circle.fill")
                    .font(.system(size: 90))
                    .foregroundColor(.blue)
                    .padding(.top, 40)
                
                // 💡 Displaying the profile text dynamically
                VStack(spacing: 4) {
                    Text(savedUsername)
                        .font(.title)
                        .bold()
                    
                    Text(savedEmail)
                        .font(.subheadline)
                        .foregroundColor(.gray)
                }
                
                Spacer()
            }
            .navigationTitle("Profile")
            .toolbar {
                ToolbarItem(placement: .topBarTrailing) {
                    Button {
                        showingSettingsSheet = true
                    } label: {
                        Image(systemName: "gearshape")
                            .font(.title3)
                    }
                }
            }
            // Displays your settings options inside a sliding model sheet popup
            .sheet(isPresented: $showingSettingsSheet) {
                SettingsView()
            }
            .navigationTitle("My Profile")
        }
    }
}


// ProfileView - E

// questionsApp - B
//
//  questionsApp.swift
//  questions
//
//  Created by Ingrid Zippe on 6/26/26.
//

import SwiftUI
import SwiftData


@main
struct questionsApp: App {
    // 1. Fetch the database context manager from the environment
    @Environment(\.modelContext) private var modelContext
    
    // 1. Initialize your global SwiftData container
    let container: ModelContainer
        
    // 💡 Read the login state globally from the device storage
    @AppStorage("isLoggedIn") private var isLoggedIn = false

    // 2. Track the logged-in user email using @AppStorage (Persisted in UserDefaults)
    @AppStorage("loggedInUserEmail") private var loggedInUserEmail: String = ""
    
    init() {
        do {
            container = try ModelContainer(for: Profile.self)
        } catch {
            fatalError("Failed to initialize ModelContainer: \(error.localizedDescription)")
        }
    }
        
    //    var sharedModelContainer: ModelContainer = {
    //        let schema = Schema([
    //            Item.self,
    //        ])
    //        let modelConfiguration = ModelConfiguration(schema: schema, isStoredInMemoryOnly: false)
    //
    //        do {
    //            return try ModelContainer(for: schema, configurations: [modelConfiguration])
    //        } catch {
    //            fatalError("Could not create ModelContainer: \(error)")
    //        }
    //    }()
        
    var body: some Scene {
        WindowGroup {
            // 3. Decide which view tree to render based on session state
            if loggedInUserEmail.isEmpty {
                LoginView()
            } else {
                // Pass the identifier down so the view can extract the model instance
                MainAppTabsView()
            }
//            // 💡 The view automatically swaps out when the boolean shifts
//            if isLoggedIn {
//                MainAppTabsView() // Your core application layout or tabs
//            } else {
//                LoginView()       // The security login wrapper
//            }
        }
        // .modelContainer(sharedModelContainer)
        .modelContainer(for: Profile.self)
    }
}


// questionsApp - E

// QuestionsView - B

//
//  QuestionsView.swift
//  questions
//
//  Created by Ingrid Zippe on 6/26/26.
//

import SwiftUI

struct QuestionsView: View {
    // 💡 Read the username string directly from local device memory
    @AppStorage("username") private var savedUsername = "Guest User"
    @AppStorage("email") private var savedEmail = "No email provided"
    
    var body: some View {
        NavigationStack {
            // 💡 Displaying the profile text dynamically
            VStack(spacing: 4) {
                Text(savedUsername)
                    .font(.title)
                    .bold()
                Text(savedEmail)
                    .font(.subheadline)
                    .foregroundColor(.gray)
            }
            .navigationTitle("Questions")
        }
    }
}


// QuestionsApp - E

// SettingsView - B

import SwiftUI

struct SettingsView: View {
    @AppStorage("isLoggedIn") private var isLoggedIn = true
    // 1. Pull the email string from AppStorage
    @AppStorage("loggedInUserEmail") private var loggedInUserEmail: String = ""
  
    var body: some View {
        Button(role: .destructive) {
            // 💡 Instantly triggers the root switch and shows LoginView again
            withAnimation {
                loggedInUserEmail = ""
                // isLoggedIn = false
            }
        } label: {
            Text("Log Out")
        }
    }
}

// SettingsView - E

// SignUpView - B

//
//  SignUpView.swift
//  questions
//
//  Created by Ingrid Zippe on 6/26/26.
//

import SwiftUI
import SwiftData

struct SignUpView: View {
    // 1. Fetch the database context manager from the environment
    @Environment(\.modelContext) private var modelContext
   // @Environment(\.dismiss) private var dismiss
    
    @State private var emailInput = ""
    @State private var passwordInput = ""
    
    // 1. Inputs and interactive states
    @State private var email = ""
    @State private var password = ""
    @State private var isLoading = false
    @State private var errorMessage: String? = nil
    
    // 💡 Must share the exact same key name string as Step 1
    @AppStorage("isLoggedIn") private var isLoggedIn = false
    
    @Query private var profiles: [Profile]
    
    // 2. Track form validity reactively
    private var isFormInvalid: Bool {
        email.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty || password.count < 6
    }
    
    var body: some View {
        NavigationStack {
            Form {
                Section("User Information") {
                    TextField("Email", text: $emailInput)
                        .keyboardType(.emailAddress)
                        .textInputAutocapitalization(.never)
                        .autocorrectionDisabled(true)
                    
                    TextField("Password", text: $passwordInput)
                        .textInputAutocapitalization(.never)
                        .autocorrectionDisabled(true)
                }
                
                Section {
                    Button {
                        saveNewUser()
                    } label: {
                        Text("Create User Account")
                            .frame(maxWidth: .infinity, alignment: .center)
                    }
                    // Disable the button if fields are blank
                    .disabled(emailInput.isEmpty || passwordInput.isEmpty)
                }
                Button("Print All Database Users") {
                    print("--- DATABASE EXPORT (Total: \(profiles.count)) ---")
                    for (index, profile) in profiles.enumerated() {
                        print("Index \(index): Email = \(profile.email), Password = \(profile.password)")
                    }
                    print("---------------------------------------")
                }
            }
            .navigationTitle("New User")
            .onAppear {
                print("📂 Database Location: \(URL.applicationSupportDirectory.path)")
            }
        }
    }
    
    private func addItem() {
        let newItem = Profile(email: emailInput, password: passwordInput)
        modelContext.insert(newItem) // Saved to disk automatically
        print(profiles)
    }
    
    // 2. The insertion logic function
    @MainActor
    private func saveNewUser() {
        // Create the new model instance matching the user inputs
        let newProfile = Profile(email: emailInput, password: passwordInput)
        
        // Tell SwiftData to insert the object into the database context
        modelContext.insert(newProfile)
        // try? modelContext.save() // Always call manual save on async completio
        
        // 2. 💡 FORCE IMMEDIATE SAVE TO DISK
        do {
            try modelContext.save()
            print("Successfully committed to SQLite disk file!")
        } catch {
            print("Database save failed: \(error.localizedDescription)")
        }
        
        // try? modelContext.save() // Always save manually when changing async states
        
        // 💡 Pro-Tip: SwiftData auto-saves when the main thread idles.
        // There is no mandatory requirement to call try? modelContext.save() manually!
        
        // dismiss() // Close the current screen or view drawer
    }
    
    // 3. Mock authentication workflow execution
    private func handleLoginAction() {
        isLoading = true
        errorMessage = nil
        
        // Simulating network authentication latency
        DispatchQueue.main.asyncAfter(deadline: .now() + 1.5) {
            isLoading = false
            
            if email == "hello@test.com" && password == "password123" {
                print("Login Success!")
                // Route user to target core App Dashboard view container
                withAnimation {
                    isLoggedIn = true
                }
            } else {
                errorMessage = "Invalid email identity or mismatching password record."
            }
        }
    }
}

#Preview {
    SignUpView()
}



// SignUpView - E

// working - END

// extra useful code

//    private func loginByQueryingDatabase() {
//        print("loginByQueryingDatabase")
//        isLoading = true
//        errorMessage = nil
//        
////        let email = emailInput.lowercased().trimmingCharacters(in: .whitespacesAndNewlines)
////        let password = passwordInput // Match exact casing/hashes
//        
//        // Simulating network authentication latency
//        DispatchQueue.main.asyncAfter(deadline: .now() + 1.5) {
//            isLoading = false
//            
//            // Define the search criteria
//            let descriptor = FetchDescriptor<Profile>(
//                predicate: #Predicate { profile in
//                    profile.email == email && profile.password == password
//                }
//            )
//            do {
//                // Execute the query
//                print("matching users")
//                let matchedUsers = try self.modelContext.fetch(descriptor)
//                
//                if let loggedInUser = matchedUsers.first {
//                    print("Login successful! Welcome \(loggedInUser.email)")
//                    withAnimation {
//                        isLoggedIn = true
//                    }
//                } else {
//                    print("Invalid email or password.")
//                }
//            } catch {
//                print("Fetch failed: \(error.localizedDescription)")
//            }
//            
////            if email == queried email && password == querie password {
////                print("Login Success!")
////                // Route user to target core App Dashboard view container
////                withAnimation {
////                    isLoggedIn = true
////                }
////            } else {
////                errorMessage = "Invalid email identity or mismatching password record."
////            }
//        }
        
//    }

//    private func checkUserData(profile: Profile) {
//        // 💡 Explicitly target the individual database attributes
//        print("👤 Email: \(profile.email) | Password: \(profile.password)")
//    }

                // Action Submission Button
                Button {
                    // handleLoginAction()
                    print("button clicked")
                    // loginByQueryingDatabase()
                } label: {
                    HStack {
                        if isLoading {
                            ProgressView()
                                .tint(.white)
                        } else {
                            Text("Sign In")
                                .bold()
                        }
                    }
                    .frame(maxWidth: .infinity)
                    .padding()
                    .background(isFormInvalid ? Color.blue : Color.gray)
                    .foregroundColor(.white)
                    .cornerRadius(12)
                }
                .disabled(isFormInvalid || isLoading) // 💡 Prevents junk API execution

// extra useful code

// working - BEGINNING - clean code - sign up working and printing from @Model on both tabs

// LoginView - B
//
//  LoginView.swift
//  questions
//
//  Created by Ingrid Zippe on 6/26/26.
//

import SwiftUI
import SwiftData

struct LoginView: View {
    // 1. Fetch the database context manager from the environment
    @Environment(\.modelContext) private var modelContext
    
    // 1. Inputs and interactive states
    @State private var email = ""
    @State private var password = ""
    @State private var isLoading = false
    @State private var errorMessage: String? = nil
    @State private var loginMessage = ""
    
    // 💡 Must share the exact same key name string as Step 1
    @AppStorage("isLoggedIn") private var isLoggedIn = false
    @AppStorage("loggedInUserEmail") private var loggedInUserEmail: String = ""
    
    @State private var navigateToSignUp = false
    @State private var navigateToMainAppTabsView = false
    
    @Query private var profiles: [Profile] 
    
    // 2. Track form validity reactively
    private var isFormInvalid: Bool {
        email.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty || password.count < 6
    }
    
    var body: some View {
        NavigationStack {
            Button("Debug Object") {
                if let firstUser = profiles.first {
                    print(firstUser)
                    // Console Output: UserModel(username: 'AlexApple', email: 'alex@test.com')
                }
            }
            VStack(spacing: 24) {
                List(profiles) { profile in
                    Text(profile.email)
                }
                Spacer()
                
                // Header Brand Section
                VStack(spacing: 8) {
                    Image(systemName: "lock.shield.fill")
                        .font(.system(size: 64))
                        .foregroundColor(.blue)
                    
                    Text("Welcome Back")
                        .font(.largeTitle)
                        .bold()
                }
                .padding(.bottom, 16)
                
                // Input Fields Section
                VStack(spacing: 16) {
                    // Email Field
                    HStack {
                        Image(systemName: "envelope")
                            .foregroundColor(.gray)
                        TextField("Email Address", text: $email)
                            .keyboardType(.emailAddress)
                            .textInputAutocapitalization(.never) // 💡 Shifts keyboard off
                            .autocorrectionDisabled(true)
                            .textContentType(.emailAddress)   // 💡 Auto-suggests saved emails
                    }
                    .padding()
                    .background(Color(.systemGray6))
                    .cornerRadius(12)
                    
                    // Password Field
                    HStack {
                        Image(systemName: "lock")
                            .foregroundColor(.gray)
                        SecureField("Password", text: $password) // 💡 Masks typed text
                            .textContentType(.password)         // 💡 Auto-fills from iCloud Keychain
                    }
                    .padding()
                    .background(Color(.systemGray6))
                    .cornerRadius(12)
                }
                
                // Forgot Password Link
                HStack {
                    Spacer()
                    Button("Forgot Password?") {
                        // Action handle
                    }
                    .font(.footnote)
                    .foregroundColor(.blue)
                }
                
                // Error feedback display
                if let errorMessage {
                    Text(errorMessage)
                        .foregroundColor(.red)
                        .font(.caption)
                        .multilineTextAlignment(.center)
                }
                Section {
                    Button("Log In", action: verifyUserCredentials)
                        .frame(maxWidth: .infinity, alignment: .center)
                        .padding()
                        .background(isFormInvalid ? Color.blue : Color.gray)
                        .foregroundColor(.white)
                        .cornerRadius(12)
                }
//                .navigationDestination(isPresented: $navigateToMainAppTabsView) {
//                    MainAppTabsView() // The screen view you want to push to [1]
//                }
                .navigationDestination(for: Profile.self) { selectedProfile in
                    MainAppTabsView()
                }
                
                if !loginMessage.isEmpty {
                    Text(loginMessage)
                        .foregroundColor(.secondary)
                    // navigateToSignUp = true
                    // MainAppTabsView()
                }
                
                Spacer()
                
                // Footer / Alternative Sign-up Link
                // 2. Your standard SwiftUI button
                Button {
                    // Perform background actions or calculations here first
                    print("Button tapped, saving data...")
                    
                    // 3. Flip the state variable to trigger the screen switch
                    navigateToSignUp = true
                } label: {
                    HStack(spacing: 4) {
                        Text("Don't have an account?")
                            .foregroundColor(.gray)
                        Text("Sign Up 2")
                            .bold()
                            .foregroundColor(.blue)
                    }
                    .font(.subheadline)
                }
                .padding(.horizontal)
                // 4. Attach the destination listener inside the NavigationStack [1]
                .navigationDestination(isPresented: $navigateToSignUp) {
                    SignUpView() // The screen view you want to push to [1]
                }
            }
            .padding(.horizontal, 24)
        }
    }
    
    func verifyUserCredentials() {
        // Clean the input to prevent matching trailing spaces or accidental uppercase inputs
        let optimizedEmail = email.trimmingCharacters(in: .whitespacesAndNewlines).lowercased()
        let typedPassword = password
        
        // 1. Build a localized query filter matching the specific credentials
        let loginDescriptor = FetchDescriptor<Profile>(
            predicate: #Predicate<Profile> { profile in
                profile.email == optimizedEmail && profile.password == typedPassword
            }
        )
        
        // 2. Limit the fetch evaluation to 1 record to save system resources
        var limitedDescriptor = loginDescriptor
        limitedDescriptor.fetchLimit = 1
        
        do {
            // 3. Look through the database
            let matchingUsers = try modelContext.fetch(limitedDescriptor)
            
            // 4. Check if a user record was returned
            if let authenticatedUser = matchingUsers.first {
                loginMessage = "Welcome back, \(authenticatedUser.email)!"
                // here
                self.loggedInUserEmail = authenticatedUser.email
                // @AppStorage("userEmail") var userEmail: String?

                navigateToMainAppTabsView = true
                // Proceed with your app navigation/session flow here
            } else {
                loginMessage = "Access denied. Invalid email or password."
            }
        } catch {
            loginMessage = "Database error: \(error.localizedDescription)"
        }
    }
    
    // 3. Mock authentication workflow execution
//    private func handleLoginAction() {
//        isLoading = true
//        errorMessage = nil
//        
//        // Simulating network authentication latency
//        DispatchQueue.main.asyncAfter(deadline: .now() + 1.5) {
//            isLoading = false
//            
//            if email == "hello@test.com" && password == "password123" {
//                print("Login Success!")
//                // Route user to target core App Dashboard view container
//                withAnimation {
//                    isLoggedIn = true
//                }
//            } else {
//                errorMessage = "Invalid email identity or mismatching password record."
//            }
//        }
//    }
    
}

#Preview {
    LoginView()
}
// LoginView - E

// MainAppTabsView - B

import SwiftUI
import SwiftData

struct MainAppTabsView: View {
    // 1. Get access to the database context for writing data
    @Environment(\.modelContext) private var modelContext
    
    // 2. Automatically fetch and sort items from the database
    @Query(sort: \TodoItem.createdAt, order: .reverse) private var items: [TodoItem]
        
    @State private var newTitle = ""
    
    @State private var selectedTab = 1
    
    @State private var showingSettingsSheet = false
    
    let email: String = ""
    
    var body: some View {
        TabView(selection: $selectedTab) {
            // --- TAB 1 ---
            NavigationStack {
                QuestionsView()
            }
            .tabItem {
                Label("Questions", systemImage: "house.fill")
            }
            .tag(1)
            // --- TAB 2 ---
            NavigationStack {
                ProfileView()
            }
            .tabItem {
                Label("Profile", systemImage: "person.fill")
            }
            .tag(2)
            
        }
    }
    // 3. Insert data into the database
    private func addItem() {
        let newItem = TodoItem(title: newTitle)
        modelContext.insert(newItem) // Saved to disk automatically
        newTitle = "" // Clear text field
        print(items)
    }
    // 4. Delete data from the database
    private func deleteItems(at offsets: IndexSet) {
        for index in offsets {
            let itemToDelete = items[index]
            modelContext.delete(itemToDelete) // Removed from disk
        }
    }
}


// MainAppTabsView - E

// Profile - B

import Foundation
import SwiftData

@Model
class Profile {
    var id = UUID()
    var email: String
    var password: String
    var createdAt: Date
    
    init(email: String, password: String, createdAt: Date = .now) {
        self.id = UUID()
        self.email = email
        self.password = password
        self.createdAt = createdAt
    }

}

// Profile - E

// ProfileView - B

//
//  ProfileView.swift
//  questions
//
//  Created by Ingrid Zippe on 6/26/26.
//

import SwiftUI
import SwiftData

struct ProfileView: View {
    // 💡 Read the username string directly from local device memory
    @AppStorage("username") private var savedUsername = "Guest User"
    @AppStorage("email") private var savedEmail = "No email provided"
    
    // 1. Pull the email string from AppStorage
    @AppStorage("loggedInUserEmail") private var loggedInUserEmail: String = ""
    
    // find the user that matches with the loggedInUserEmail
    // 2. Declare the query property (do not assign a filter here)
    @Query private var matchedUsers: [Profile]
    // 3. Create a custom initializer to read the AppStorage string
    init() {
        // Fetch the value directly from UserDefaults during initialization
        let savedEmail = UserDefaults.standard.string(forKey: "loggedInUserEmail") ?? ""
        let cleanEmail = savedEmail.lowercased().trimmingCharacters(in: .whitespacesAndNewlines)
        // Build the database search filter
        let filter = #Predicate<Profile> { profile in
            profile.email == cleanEmail
        }
        // Inject the filter back into the underlying @Query property wrapper
        _matchedUsers = Query(filter: filter)
    }
    
    @State private var showingSettingsSheet = false
    
    // let profile: Profile
    
    var body: some View {
        NavigationStack {
            Group {
                // 4. Safely unwrap the first matching database object
                if let currentUser = matchedUsers.first {
                    // Profile Avatar Frame
                    Image(systemName: "person.crop.circle.fill")
                        .font(.system(size: 90))
                        .foregroundColor(.blue)
                        .padding(.top, 40)
                    Form {
                        Section("Profile Information") {
                            Text("Email: \(currentUser.email)")
//                                .font(.title)
//                                .bold()
                            // Editable fields automatically sync with SwiftData
                            TextField("Email", text: Bindable(currentUser).email)
                        }
                        
                        Button("Log Out", role: .destructive) {
                            // Clearing this instantly updates the UI layout state
                            loggedInUserEmail = ""
                        }
                    }
                } else {
                    // Fallback loader if the database is lagging or the user was deleted
                    ContentUnavailableView(
                        "Profile Not Found",
                        systemImage: "person.crop.circle.badge.exclamationmark",
                        description: Text("Please try logging in again.")
                    )
                }
            }
            .navigationTitle("Dashboard")
            .toolbar {
                ToolbarItem(placement: .topBarTrailing) {
                    Button {
                        showingSettingsSheet = true
                    } label: {
                        Image(systemName: "gearshape")
                            .font(.title3)
                    }
                }
            }
            // Displays your settings options inside a sliding model sheet popup
            .sheet(isPresented: $showingSettingsSheet) {
                SettingsView()
            }
            .navigationTitle("My Profile")
        }
    }
}


// ProfileView - E

// questionsApp - B

//
//  questionsApp.swift
//  questions
//
//  Created by Ingrid Zippe on 6/26/26.
//

import SwiftUI
import SwiftData


@main
struct questionsApp: App {
    // 1. Fetch the database context manager from the environment
    @Environment(\.modelContext) private var modelContext
    
    // 1. Initialize your global SwiftData container
    let container: ModelContainer
        
    // 💡 Read the login state globally from the device storage
    @AppStorage("isLoggedIn") private var isLoggedIn = false

    // 2. Track the logged-in user email using @AppStorage (Persisted in UserDefaults)
    @AppStorage("loggedInUserEmail") private var loggedInUserEmail: String = ""
    
    init() {
        do {
            container = try ModelContainer(for: Profile.self)
        } catch {
            fatalError("Failed to initialize ModelContainer: \(error.localizedDescription)")
        }
    }
        
    //    var sharedModelContainer: ModelContainer = {
    //        let schema = Schema([
    //            Item.self,
    //        ])
    //        let modelConfiguration = ModelConfiguration(schema: schema, isStoredInMemoryOnly: false)
    //
    //        do {
    //            return try ModelContainer(for: schema, configurations: [modelConfiguration])
    //        } catch {
    //            fatalError("Could not create ModelContainer: \(error)")
    //        }
    //    }()
        
    var body: some Scene {
        WindowGroup {
            // 3. Decide which view tree to render based on session state
            if loggedInUserEmail.isEmpty {
                LoginView()
            } else {
                // Pass the identifier down so the view can extract the model instance
                MainAppTabsView()
            }
//            // 💡 The view automatically swaps out when the boolean shifts
//            if isLoggedIn {
//                MainAppTabsView() // Your core application layout or tabs
//            } else {
//                LoginView()       // The security login wrapper
//            }
        }
        // .modelContainer(sharedModelContainer)
        .modelContainer(for: Profile.self)
    }
}


// questionsApp - E

// QuestionsView - B

//
//  QuestionsView.swift
//  questions
//
//  Created by Ingrid Zippe on 6/26/26.
//

import SwiftUI
import SwiftData

struct QuestionsView: View {
    // 💡 Read the username string directly from local device memory
    @AppStorage("username") private var savedUsername = "Guest User"
    @AppStorage("email") private var savedEmail = "No email provided"
    
    // 1. Pull the email string from AppStorage
    @AppStorage("loggedInUserEmail") private var loggedInUserEmail: String = ""
    
    // find the user that matches with the loggedInUserEmail
    // 2. Declare the query property (do not assign a filter here)
    @Query private var matchedUsers: [Profile]
    // 3. Create a custom initializer to read the AppStorage string
    init() {
        // Fetch the value directly from UserDefaults during initialization
        let savedEmail = UserDefaults.standard.string(forKey: "loggedInUserEmail") ?? ""
        let cleanEmail = savedEmail.lowercased().trimmingCharacters(in: .whitespacesAndNewlines)
        // Build the database search filter
        let filter = #Predicate<Profile> { profile in
            profile.email == cleanEmail
        }
        // Inject the filter back into the underlying @Query property wrapper
        _matchedUsers = Query(filter: filter)
    }
    
    var body: some View {
        NavigationStack {
            Group {
                // 4. Safely unwrap the first matching database object
                if let currentUser = matchedUsers.first {
                    Form {
                        Section("Profile Information") {
                            Text("Email: \(currentUser.email)")
                            //                                .font(.title)
                            //                                .bold()
                            // Editable fields automatically sync with SwiftData
                            TextField("Email", text: Bindable(currentUser).email)
                        }
                    }
                } else {
                    // Fallback loader if the database is lagging or the user was deleted
                    ContentUnavailableView(
                        "Profile Not Found",
                        systemImage: "person.crop.circle.badge.exclamationmark",
                        description: Text("Please try logging in again.")
                    )
                }
            }
            .navigationTitle("Questions")
        }
    }
}


// QuestionsView - E

// SettingsView - B
import SwiftUI

struct SettingsView: View {
    // @AppStorage("isLoggedIn") private var isLoggedIn = true
    // 1. Pull the email string from AppStorage
    @AppStorage("loggedInUserEmail") private var loggedInUserEmail: String = ""
  
    var body: some View {
        Button(role: .destructive) {
            // 💡 Instantly triggers the root switch and shows LoginView again
            withAnimation {
                loggedInUserEmail = ""
                // isLoggedIn = false
            }
        } label: {
            Text("Log Out")
        }
    }
}
// SettingsView - E

// SignUpView - B

//
//  SignUpView.swift
//  questions
//
//  Created by Ingrid Zippe on 6/26/26.
//

import SwiftUI
import SwiftData

struct SignUpView: View {
    // 1. Fetch the database context manager from the environment
    @Environment(\.modelContext) private var modelContext
   // @Environment(\.dismiss) private var dismiss
    
    @State private var emailInput = ""
    @State private var passwordInput = ""
    
    // 1. Inputs and interactive states
    @State private var email = ""
    @State private var password = ""
    @State private var isLoading = false
    @State private var errorMessage: String? = nil
    
    // 💡 Must share the exact same key name string as Step 1
    @AppStorage("isLoggedIn") private var isLoggedIn = false
    
    @Query private var profiles: [Profile]
    
    // 2. Track form validity reactively
    private var isFormInvalid: Bool {
        email.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty || password.count < 6
    }
    
    var body: some View {
        NavigationStack {
            Form {
                Section("User Information") {
                    TextField("Email", text: $emailInput)
                        .keyboardType(.emailAddress)
                        .textInputAutocapitalization(.never)
                        .autocorrectionDisabled(true)
                    
                    TextField("Password", text: $passwordInput)
                        .textInputAutocapitalization(.never)
                        .autocorrectionDisabled(true)
                }
                
                Section {
                    Button {
                        saveNewUser()
                    } label: {
                        Text("Create User Account")
                            .frame(maxWidth: .infinity, alignment: .center)
                    }
                    // Disable the button if fields are blank
                    .disabled(emailInput.isEmpty || passwordInput.isEmpty)
                }
                Button("Print All Database Users") {
                    print("--- DATABASE EXPORT (Total: \(profiles.count)) ---")
                    for (index, profile) in profiles.enumerated() {
                        print("Index \(index): Email = \(profile.email), Password = \(profile.password)")
                    }
                    print("---------------------------------------")
                }
            }
            .navigationTitle("New User")
            .onAppear {
                print("📂 Database Location: \(URL.applicationSupportDirectory.path)")
            }
        }
    }
    
//    private func addItem() {
//        let newItem = Profile(email: emailInput, password: passwordInput)
//        modelContext.insert(newItem) // Saved to disk automatically
//        print(profiles)
//    }
    
    // 2. The insertion logic function
    @MainActor
    private func saveNewUser() {
        // Create the new model instance matching the user inputs
        let newProfile = Profile(email: emailInput, password: passwordInput)
        
        // Tell SwiftData to insert the object into the database context
        modelContext.insert(newProfile)
        // try? modelContext.save() // Always call manual save on async completio
        
        // 2. 💡 FORCE IMMEDIATE SAVE TO DISK
        do {
            try modelContext.save()
            print("Successfully committed to SQLite disk file!")
        } catch {
            print("Database save failed: \(error.localizedDescription)")
        }
        
        // try? modelContext.save() // Always save manually when changing async states
        
        // 💡 Pro-Tip: SwiftData auto-saves when the main thread idles.
        // There is no mandatory requirement to call try? modelContext.save() manually!
        
        // dismiss() // Close the current screen or view drawer
    }
    
    // 3. Mock authentication workflow execution
    private func handleLoginAction() {
        isLoading = true
        errorMessage = nil
        
        // Simulating network authentication latency
        DispatchQueue.main.asyncAfter(deadline: .now() + 1.5) {
            isLoading = false
            
            if email == "hello@test.com" && password == "password123" {
                print("Login Success!")
                // Route user to target core App Dashboard view container
                withAnimation {
                    isLoggedIn = true
                }
            } else {
                errorMessage = "Invalid email identity or mismatching password record."
            }
        }
    }
}

#Preview {
    SignUpView()
}



// SignUpView - E

// working - END


import Foundation
import SwiftData

// 1. Create a conforming Codable struct
struct Answer: Codable {
    var id = UUID()
    var createdAt = Date
    var question = String
    var userEmail: String
    var answer: String
    var price: Int
}

@Model
class Question {
    var id: UUID()
    var createdAt: Date
    // 1. Mark the email as unique to ensure data integrity
    @Attribute(.unique) var loggedInUserEmail: String
    var price: Int
    var tags: [String]
    var question: String
    var answers: [Answer]

favoriteTags: [String] = []

    init(name: String, createdAt: Date = .now, loggedInUserEmail: String, price = Int, tags: [String] = [], question = String, answers: [Answer] = []) {
        self.id = UUID()
        self.createdAt = createdAt
        // Clean text to prevent uppercase variations or trailing whitespace bugs
        self.loggedInUserEmail = loggedInUserEmail.trimmingCharacters(in: .whitespacesAndNewlines).lowercased()
        self.price = price
        self.tags = tags
        self.question = question
        self.answers = answers
    }
}

import Foundation
import SwiftData

@Model
class Profile {
    var id = UUID()
    var email: String
    var password: String
    var createdAt: Date
    
    init(email: String, password: String, createdAt: Date = .now) {
        self.id = UUID()
        self.email = email
        self.password = password
        self.createdAt = createdAt
    }

}

Profile 
    id
    email
    password
    createdAt

Question
    id
    createdAt
    userEmail
    price
    tags
    question
    answers


// working - BEGINNING - saves question and prints question on QuestionsView

// LoginView - B

//
//  LoginView.swift
//  questions
//
//  Created by Ingrid Zippe on 6/26/26.
//

import SwiftUI
import SwiftData

struct LoginView: View {
    // 1. Fetch the database context manager from the environment
    @Environment(\.modelContext) private var modelContext
    
    // 1. Inputs and interactive states
    @State private var email = ""
    @State private var password = ""
    @State private var isLoading = false
    @State private var errorMessage: String? = nil
    @State private var loginMessage = ""
    
    // 💡 Must share the exact same key name string as Step 1
    @AppStorage("isLoggedIn") private var isLoggedIn = false
    @AppStorage("loggedInUserEmail") private var loggedInUserEmail: String = ""
    
    @State private var navigateToSignUp = false
    @State private var navigateToMainAppTabsView = false
    
    @Query private var profiles: [Profile] 
    
    // 2. Track form validity reactively
    private var isFormInvalid: Bool {
        email.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty || password.count < 6
    }
    
    var body: some View {
        NavigationStack {
            Button("Debug Object") {
                if let firstUser = profiles.first {
                    print(firstUser)
                    // Console Output: UserModel(username: 'AlexApple', email: 'alex@test.com')
                }
            }
            VStack(spacing: 24) {
                List(profiles) { profile in
                    Text(profile.email)
                }
                Spacer()
                
                // Header Brand Section
                VStack(spacing: 8) {
                    Image(systemName: "lock.shield.fill")
                        .font(.system(size: 64))
                        .foregroundColor(.blue)
                    
                    Text("Welcome Back")
                        .font(.largeTitle)
                        .bold()
                }
                .padding(.bottom, 16)
                
                // Input Fields Section
                VStack(spacing: 16) {
                    // Email Field
                    HStack {
                        Image(systemName: "envelope")
                            .foregroundColor(.gray)
                        TextField("Email Address", text: $email)
                            .keyboardType(.emailAddress)
                            .textInputAutocapitalization(.never) // 💡 Shifts keyboard off
                            .autocorrectionDisabled(true)
                            .textContentType(.emailAddress)   // 💡 Auto-suggests saved emails
                    }
                    .padding()
                    .background(Color(.systemGray6))
                    .cornerRadius(12)
                    
                    // Password Field
                    HStack {
                        Image(systemName: "lock")
                            .foregroundColor(.gray)
                        SecureField("Password", text: $password) // 💡 Masks typed text
                            .textContentType(.password)         // 💡 Auto-fills from iCloud Keychain
                    }
                    .padding()
                    .background(Color(.systemGray6))
                    .cornerRadius(12)
                }
                
                // Forgot Password Link
                HStack {
                    Spacer()
                    Button("Forgot Password?") {
                        // Action handle
                    }
                    .font(.footnote)
                    .foregroundColor(.blue)
                }
                
                // Error feedback display
                if let errorMessage {
                    Text(errorMessage)
                        .foregroundColor(.red)
                        .font(.caption)
                        .multilineTextAlignment(.center)
                }
                Section {
                    Button("Log In", action: verifyUserCredentials)
                        .frame(maxWidth: .infinity, alignment: .center)
                        .padding()
                        .background(isFormInvalid ? Color.blue : Color.gray)
                        .foregroundColor(.white)
                        .cornerRadius(12)
                }
//                .navigationDestination(isPresented: $navigateToMainAppTabsView) {
//                    MainAppTabsView() // The screen view you want to push to [1]
//                }
                .navigationDestination(for: Profile.self) { selectedProfile in
                    MainAppTabsView()
                }
                
                if !loginMessage.isEmpty {
                    Text(loginMessage)
                        .foregroundColor(.secondary)
                    // navigateToSignUp = true
                    // MainAppTabsView()
                }
                
                Spacer()
                
                // Footer / Alternative Sign-up Link
                // 2. Your standard SwiftUI button
                Button {
                    // Perform background actions or calculations here first
                    print("Button tapped, saving data...")
                    
                    // 3. Flip the state variable to trigger the screen switch
                    navigateToSignUp = true
                } label: {
                    HStack(spacing: 4) {
                        Text("Don't have an account?")
                            .foregroundColor(.gray)
                        Text("Sign Up 2")
                            .bold()
                            .foregroundColor(.blue)
                    }
                    .font(.subheadline)
                }
                .padding(.horizontal)
                // 4. Attach the destination listener inside the NavigationStack [1]
                .navigationDestination(isPresented: $navigateToSignUp) {
                    SignUpView() // The screen view you want to push to [1]
                }
            }
            .padding(.horizontal, 24)
        }
    }
    
    func verifyUserCredentials() {
        // Clean the input to prevent matching trailing spaces or accidental uppercase inputs
        let optimizedEmail = email.trimmingCharacters(in: .whitespacesAndNewlines).lowercased()
        let typedPassword = password
        
        // 1. Build a localized query filter matching the specific credentials
        let loginDescriptor = FetchDescriptor<Profile>(
            predicate: #Predicate<Profile> { profile in
                profile.email == optimizedEmail && profile.password == typedPassword
            }
        )
        
        // 2. Limit the fetch evaluation to 1 record to save system resources
        var limitedDescriptor = loginDescriptor
        limitedDescriptor.fetchLimit = 1
        
        do {
            // 3. Look through the database
            let matchingUsers = try modelContext.fetch(limitedDescriptor)
            
            // 4. Check if a user record was returned
            if let authenticatedUser = matchingUsers.first {
                loginMessage = "Welcome back, \(authenticatedUser.email)!"
                // here
                self.loggedInUserEmail = authenticatedUser.email
                // @AppStorage("userEmail") var userEmail: String?

                navigateToMainAppTabsView = true
                // Proceed with your app navigation/session flow here
            } else {
                loginMessage = "Access denied. Invalid email or password."
            }
        } catch {
            loginMessage = "Database error: \(error.localizedDescription)"
        }
    }
    
    // 3. Mock authentication workflow execution
//    private func handleLoginAction() {
//        isLoading = true
//        errorMessage = nil
//        
//        // Simulating network authentication latency
//        DispatchQueue.main.asyncAfter(deadline: .now() + 1.5) {
//            isLoading = false
//            
//            if email == "hello@test.com" && password == "password123" {
//                print("Login Success!")
//                // Route user to target core App Dashboard view container
//                withAnimation {
//                    isLoggedIn = true
//                }
//            } else {
//                errorMessage = "Invalid email identity or mismatching password record."
//            }
//        }
//    }
    
}

#Preview {
    LoginView()
}

// LoginView - E

// MainAppTabsView - B

import SwiftUI
import SwiftData

struct MainAppTabsView: View {
    // 1. Get access to the database context for writing data
    @Environment(\.modelContext) private var modelContext
    
    // 2. Automatically fetch and sort items from the database
    @Query(sort: \TodoItem.createdAt, order: .reverse) private var items: [TodoItem]
        
    @State private var newTitle = ""
    
    @State private var selectedTab = 1
    
    @State private var showingSettingsSheet = false
    
    let email: String = ""
    
    var body: some View {
        TabView(selection: $selectedTab) {
            // --- TAB 1 ---
            NavigationStack {
                QuestionsView()
            }
            .tabItem {
                Label("Questions", systemImage: "house.fill")
            }
            .tag(1)
            // --- TAB 2 ---
            NavigationStack {
                ProfileView()
            }
            .tabItem {
                Label("Profile", systemImage: "person.fill")
            }
            .tag(2)
            
        }
    }
    // 3. Insert data into the database
    private func addItem() {
        let newItem = TodoItem(title: newTitle)
        modelContext.insert(newItem) // Saved to disk automatically
        newTitle = "" // Clear text field
        print(items)
    }
    // 4. Delete data from the database
    private func deleteItems(at offsets: IndexSet) {
        for index in offsets {
            let itemToDelete = items[index]
            modelContext.delete(itemToDelete) // Removed from disk
        }
    }
}


// MainAppTabsView - E

// Profile - B

//
//  Profile.swift
//  questions
//
//  Created by Ingrid Zippe on 6/26/26.
//

import Foundation
import SwiftData

@Model
class Profile {
    var id = UUID()
    var email: String
    var password: String
    var createdAt: Date
    
    init(email: String, password: String, createdAt: Date = .now) {
        self.id = UUID()
        self.email = email
        self.password = password
        self.createdAt = createdAt
    }
}
    


// Profile - E

// ProfileView - B

//
//  ProfileView.swift
//  questions
//
//  Created by Ingrid Zippe on 6/26/26.
//

import SwiftUI
import SwiftData

struct ProfileView: View {
    // 💡 Read the username string directly from local device memory
    @AppStorage("username") private var savedUsername = "Guest User"
    @AppStorage("email") private var savedEmail = "No email provided"
    
    // 1. Pull the email string from AppStorage
    @AppStorage("loggedInUserEmail") private var loggedInUserEmail: String = ""
    
    // find the user that matches with the loggedInUserEmail
    // 2. Declare the query property (do not assign a filter here)
    @Query private var matchedUsers: [Profile]
    // 3. Create a custom initializer to read the AppStorage string
    init() {
        // Fetch the value directly from UserDefaults during initialization
        let savedEmail = UserDefaults.standard.string(forKey: "loggedInUserEmail") ?? ""
        let cleanEmail = savedEmail.lowercased().trimmingCharacters(in: .whitespacesAndNewlines)
        // Build the database search filter
        let filter = #Predicate<Profile> { profile in
            profile.email == cleanEmail
        }
        // Inject the filter back into the underlying @Query property wrapper
        _matchedUsers = Query(filter: filter)
    }
    
    @State private var showingSettingsSheet = false
    
    // 1. Declare the query variable without a filter assignment
    @Query private var filteredQuestions: [Question]

    // 2. Accept the target email search string in the initializer
    init(targetEmail: String) {
        let cleanEmail = targetEmail.trimmingCharacters(in: .whitespacesAndNewlines).lowercased()
        // 3. Construct the relational filter predicate
        let searchPredicate = #Predicate<Question> { question in
            question.loggedInUserEmail == cleanEmail
        }
        // 4. Inject the generated predicate into the underlying Query property wrapper
        _filteredQuestions = Query(filter: searchPredicate)
        print(filteredQuestions)
    }
    
    // let profile: Profile
    
    var body: some View {
        NavigationStack {
            Group {
                List(filteredQuestions) { question in
                    VStack(alignment: .leading) {
                        Text(question.loggedInUserEmail)
                            .font(.headline)
                        Text(question.question)
                            .font(.subheadline)
                            .foregroundColor(.secondary)
                    }
                }
                // 4. Safely unwrap the first matching database object
                if let currentUser = matchedUsers.first {
                    // Profile Avatar Frame
                    Image(systemName: "person.crop.circle.fill")
                        .font(.system(size: 90))
                        .foregroundColor(.blue)
                        .padding(.top, 40)
                    Form {
                        Section("Profile Information") {
                            Text("Email: \(currentUser.email)")
//                                .font(.title)
//                                .bold()
                            // Editable fields automatically sync with SwiftData
                            TextField("Email", text: Bindable(currentUser).email)
                        }
                        
                        Button("Log Out", role: .destructive) {
                            // Clearing this instantly updates the UI layout state
                            loggedInUserEmail = ""
                        }
                    }
                } else {
                    // Fallback loader if the database is lagging or the user was deleted
                    ContentUnavailableView(
                        "Profile Not Found",
                        systemImage: "person.crop.circle.badge.exclamationmark",
                        description: Text("Please try logging in again.")
                    )
                }
            }
            .navigationTitle("Dashboard")
            .toolbar {
                ToolbarItem(placement: .topBarTrailing) {
                    Button {
                        showingSettingsSheet = true
                    } label: {
                        Image(systemName: "gearshape")
                            .font(.title3)
                    }
                }
            }
            // Displays your settings options inside a sliding model sheet popup
            .sheet(isPresented: $showingSettingsSheet) {
                SettingsView()
            }
            .navigationTitle("My Profile")
        }
    }
}


// ProfileView - E

// Question - B

//
//  Question.swift
//  questions
//
//  Created by Ingrid Zippe on 6/27/26.
//
import Foundation
import SwiftData

// 1. Create a conforming Codable struct
//struct Answer: Codable {
//    var id = UUID()
//    var createdAt = Date
//    var question = String
//    var userEmail: String
//    var answer: String
//    var price: Int
//}

@Model
class Question {
    var id = UUID()
    var createdAt: Date
    // 1. Mark the email as unique to ensure data integrity
    var loggedInUserEmail: String
    var price: Double
    var tags: [String]
    var question: String
    // var answers: [Answer]

//    init(name: String, createdAt: Date = .now, loggedInUserEmail: String, price: Double, tags: [String] = [], question: String, answers: [Answer] = []) {
    init(createdAt: Date = .now, loggedInUserEmail: String, price: Double, tags: [String] = [], question: String) {
        self.id = UUID()
        self.createdAt = createdAt
        // Clean text to prevent uppercase variations or trailing whitespace bugs
        self.loggedInUserEmail = loggedInUserEmail.trimmingCharacters(in: .whitespacesAndNewlines).lowercased()
        self.price = price
        self.tags = tags
        self.question = question
        // self.answers = answers
    }
}


// Question - E

// questionsApp - B

//
//  questionsApp.swift
//  questions
//
//  Created by Ingrid Zippe on 6/26/26.
//

import SwiftUI
import SwiftData


@main
struct questionsApp: App {
    // 1. Fetch the database context manager from the environment
    @Environment(\.modelContext) private var modelContext
    
    // 1. Initialize your global SwiftData container
    let container: ModelContainer
        
    // 💡 Read the login state globally from the device storage
    @AppStorage("isLoggedIn") private var isLoggedIn = false

    // 2. Track the logged-in user email using @AppStorage (Persisted in UserDefaults)
    @AppStorage("loggedInUserEmail") private var loggedInUserEmail: String = ""
    
    init() {
        do {
            container = try ModelContainer(for: Profile.self)
        } catch {
            fatalError("Failed to initialize ModelContainer: \(error.localizedDescription)")
        }
    }
        
    //    var sharedModelContainer: ModelContainer = {
    //        let schema = Schema([
    //            Item.self,
    //        ])
    //        let modelConfiguration = ModelConfiguration(schema: schema, isStoredInMemoryOnly: false)
    //
    //        do {
    //            return try ModelContainer(for: schema, configurations: [modelConfiguration])
    //        } catch {
    //            fatalError("Could not create ModelContainer: \(error)")
    //        }
    //    }()
        
    var body: some Scene {
        WindowGroup {
            // 3. Decide which view tree to render based on session state
            if loggedInUserEmail.isEmpty {
                LoginView()
            } else {
                // Pass the identifier down so the view can extract the model instance
                MainAppTabsView()
            }
//            // 💡 The view automatically swaps out when the boolean shifts
//            if isLoggedIn {
//                MainAppTabsView() // Your core application layout or tabs
//            } else {
//                LoginView()       // The security login wrapper
//            }
        }
        // .modelContainer(sharedModelContainer)
        .modelContainer(for: [Profile.self, Question.self])
    }
}


// questionsApp - E

// QuestionsView - B

//
//  QuestionsView.swift
//  questions
//
//  Created by Ingrid Zippe on 6/26/26.
//

import SwiftUI
import SwiftData

struct QuestionsView: View {
    // 1. Access the database context
    @Environment(\.modelContext) private var modelContext
    
    // 💡 Read the username string directly from local device memory
    @AppStorage("username") private var savedUsername = "Guest User"
    @AppStorage("email") private var savedEmail = "No email provided"
    
    // 1. Pull the email string from AppStorage
    @AppStorage("loggedInUserEmail") private var loggedInUserEmail: String = ""
    
    // 2. Local state to hold the live text field input
    @State private var questionInput: String = ""
    @State private var questionPriceInput: Double = 0.0
    
    // find the user that matches with the loggedInUserEmail
    // 2. Declare the query property (do not assign a filter here)
    @Query private var matchedUsers: [Profile]
    // 3. Create a custom initializer to read the AppStorage string
    init() {
        // Fetch the value directly from UserDefaults during initialization
        let savedEmail = UserDefaults.standard.string(forKey: "loggedInUserEmail") ?? ""
        let cleanEmail = savedEmail.lowercased().trimmingCharacters(in: .whitespacesAndNewlines)
        // Build the database search filter
        let filter = #Predicate<Profile> { profile in
            profile.email == cleanEmail
        }
        // Inject the filter back into the underlying @Query property wrapper
        _matchedUsers = Query(filter: filter)
    }
    
    @Query private var questions: [Question]
    
//    // Queries only users with the specific admin email address
//    @Query(filter: #Predicate<Question> { question in
//        question.loggedInUserEmail == loggedInUserEmail
//    })
//    private var questionsList: [Question]
    
    
    var body: some View {
        NavigationStack {
            List(questions) { question in
                Text(question.loggedInUserEmail)
                    .font(Font.body.bold())
                Text(question.question)
            }
            Group {
//                // 4. Safely unwrap the first matching database object
//                if let currentUser = matchedUsers.first {
//                    Form {
//                        Section("Profile Information") {
//                            Text("Email: \(currentUser.email)")
//                            //                                .font(.title)
//                            //                                .bold()
//                            // Editable fields automatically sync with SwiftData
//                            TextField("Email", text: Bindable(currentUser).email)
//                        }
//                    }
//                } else {
//                    // Fallback loader if the database is lagging or the user was deleted
//                    ContentUnavailableView(
//                        "Profile Not Found",
//                        systemImage: "person.crop.circle.badge.exclamationmark",
//                        description: Text("Please try logging in again.")
//                    )
//                }
                Form {
                    Section("Ask A Question") {
                        TextField("Your question...", text: $questionInput)
                            .textFieldStyle(.plain)
                        TextField("Question Price", value: $questionPriceInput, format: .currency(code: "USD"))
                            .keyboardType(.decimalPad)
                    }
                    
                    Section {
                        Button(action: submitQuestionToDatabase) {
                            Text("Submit Your Question")
                                .frame(maxWidth: .infinity, alignment: .center)
                                .bold()
                        }
                        // Disable the button if the user hasn't typed anything
                        .disabled(questionInput.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty)
                    }
                }
                .navigationTitle("Questions")
                Form {
                    Button("Print All Database Users") {
                        print("--- DATABASE EXPORT (Total: \(questions.count)) ---")
                        for (index, question) in questions.enumerated() {
                            print("Index \(index): Email = \(question.loggedInUserEmail), Question = \(question.question)")
                        }
                        print("---------------------------------------")
                    }
                    
                }
            }
            .navigationTitle("Questions")
        }
    }
    // 3. The submission handler
    @MainActor
    func submitQuestionToDatabase() {
        // Clean the input text
        let cleanText = questionInput.trimmingCharacters(in: .whitespacesAndNewlines)
        
        // Ensure data isn't empty after trimming
        guard !cleanText.isEmpty else { return }
        
        // 4. Create an instance of your SwiftData @Model class
        let newQuestion = Question(loggedInUserEmail: loggedInUserEmail, price: questionPriceInput, question: cleanText)
        
        // 5. Insert the object into the database context
        modelContext.insert(newQuestion)
        
        // 6. Force an immediate write to disk so changes aren't lost on app exit
        do {
            try modelContext.save()
            
            // 7. Reset the input field UI back to blank upon success
            questionInput = ""
            print("Saved to SwiftData!")
            
        } catch {
            print("Failed to save to SwiftData: \(error.localizedDescription)")
        }
    }
}


// QuestionsView - E

// SettingsView - B

//
//  SettingsView.swift
//  questions
//
//  Created by Ingrid Zippe on 6/26/26.
//

import SwiftUI

struct SettingsView: View {
    // @AppStorage("isLoggedIn") private var isLoggedIn = true
    // 1. Pull the email string from AppStorage
    @AppStorage("loggedInUserEmail") private var loggedInUserEmail: String = ""
  
    var body: some View {
        Button(role: .destructive) {
            // 💡 Instantly triggers the root switch and shows LoginView again
            withAnimation {
                loggedInUserEmail = ""
                // isLoggedIn = false
            }
        } label: {
            Text("Log Out")
        }
    }
}


// SettingsView - E

// SignUpView - B

//
//  SignUpView.swift
//  questions
//
//  Created by Ingrid Zippe on 6/26/26.
//

import SwiftUI
import SwiftData

struct SignUpView: View {
    // 1. Fetch the database context manager from the environment
    @Environment(\.modelContext) private var modelContext
   // @Environment(\.dismiss) private var dismiss
    
    @State private var emailInput = ""
    @State private var passwordInput = ""
    
    // 1. Inputs and interactive states
    @State private var email = ""
    @State private var password = ""
    @State private var isLoading = false
    @State private var errorMessage: String? = nil
    
    // 💡 Must share the exact same key name string as Step 1
    @AppStorage("isLoggedIn") private var isLoggedIn = false
    
    @Query private var profiles: [Profile]
    
    // 2. Track form validity reactively
    private var isFormInvalid: Bool {
        email.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty || password.count < 6
    }
    
    var body: some View {
        NavigationStack {
            Form {
                Section("User Information") {
                    TextField("Email", text: $emailInput)
                        .keyboardType(.emailAddress)
                        .textInputAutocapitalization(.never)
                        .autocorrectionDisabled(true)
                    
                    TextField("Password", text: $passwordInput)
                        .textInputAutocapitalization(.never)
                        .autocorrectionDisabled(true)
                }
                
                Section {
                    Button {
                        saveNewUser()
                    } label: {
                        Text("Create User Account")
                            .frame(maxWidth: .infinity, alignment: .center)
                    }
                    // Disable the button if fields are blank
                    .disabled(emailInput.isEmpty || passwordInput.isEmpty)
                }
                Button("Print All Database Users") {
                    print("--- DATABASE EXPORT (Total: \(profiles.count)) ---")
                    for (index, profile) in profiles.enumerated() {
                        print("Index \(index): Email = \(profile.email), Password = \(profile.password)")
                    }
                    print("---------------------------------------")
                }
            }
            .navigationTitle("New User")
            .onAppear {
                print("📂 Database Location: \(URL.applicationSupportDirectory.path)")
            }
        }
    }
    
//    private func addItem() {
//        let newItem = Profile(email: emailInput, password: passwordInput)
//        modelContext.insert(newItem) // Saved to disk automatically
//        print(profiles)
//    }
    
    // 2. The insertion logic function
    @MainActor
    private func saveNewUser() {
        // Create the new model instance matching the user inputs
        let newProfile = Profile(email: emailInput, password: passwordInput)
        
        // Tell SwiftData to insert the object into the database context
        modelContext.insert(newProfile)
        // try? modelContext.save() // Always call manual save on async completio
        
        // 2. 💡 FORCE IMMEDIATE SAVE TO DISK
        do {
            try modelContext.save()
            print("Successfully committed to SQLite disk file!")
        } catch {
            print("Database save failed: \(error.localizedDescription)")
        }
        
        // try? modelContext.save() // Always save manually when changing async states
        
        // 💡 Pro-Tip: SwiftData auto-saves when the main thread idles.
        // There is no mandatory requirement to call try? modelContext.save() manually!
        
        // dismiss() // Close the current screen or view drawer
    }
    
    // 3. Mock authentication workflow execution
    private func handleLoginAction() {
        isLoading = true
        errorMessage = nil
        
        // Simulating network authentication latency
        DispatchQueue.main.asyncAfter(deadline: .now() + 1.5) {
            isLoading = false
            
            if email == "hello@test.com" && password == "password123" {
                print("Login Success!")
                // Route user to target core App Dashboard view container
                withAnimation {
                    isLoggedIn = true
                }
            } else {
                errorMessage = "Invalid email identity or mismatching password record."
            }
        }
    }
}

#Preview {
    SignUpView()
}



// SignUpView - E

// working - END






// LoginView - B

// LoginView - E

// MainAppTabsView - B

// MainAppTabsView - E

// Profile - B

// Profile - E

// ProfileView - B

// ProfileView - E

// Question - B

import Foundation
import SwiftData


// 1. Create a conforming Codable struct
//struct Answer: Codable {
//    var id = UUID()
//    var createdAt = Date
//    var question = String
//    var userEmail: String
//    var answer: String
//    var price: Int
//}
@Model
class Answer {
    var id = UUID()
    var createdAt: Date
    var question: String
    var loggedInUserEmail: String
    var answer: String
    var answerPrice: Double
    
    init(createdAt: Date = .now, question: String, loggedInUserEmail: String, answer: String, answerPrice: Double) {
        self.id = UUID()
        self.createdAt = createdAt
        self.question = question
        self.loggedInUserEmail = loggedInUserEmail.trimmingCharacters(in: .whitespacesAndNewlines).lowercased()
        self.answer = answer
        self.answerPrice = answerPrice
    }
}

@Model
class Question {
    var id = UUID()
    var createdAt: Date
    // 1. Mark the email as unique to ensure data integrity
    var loggedInUserEmail: String
    var price: Double
    var tags: [String]
    var question: String
    
    // back pointer relationship reference linking back to parent
    var answers: Answer?

    init(createdAt: Date = .now, loggedInUserEmail: String, price: Double, tags: [String] = [], question: String, answers: [Answer]) {
    // init(createdAt: Date = .now, loggedInUserEmail: String, price: Double, tags: [String] = [], question: String) {
        self.id = UUID()
        self.createdAt = createdAt
        // Clean text to prevent uppercase variations or trailing whitespace bugs
        self.loggedInUserEmail = loggedInUserEmail.trimmingCharacters(in: .whitespacesAndNewlines).lowercased()
        self.price = price
        self.tags = tags
        self.question = question
        // self.answers = answers
    }
}

// Question - E

// questionsApp - B

// questionsApp - E

// QuestionsView - B

import SwiftUI
import SwiftData

struct QuestionsView: View {
    // 1. Access the database context
    @Environment(\.modelContext) private var modelContext
    
    // 💡 Read the username string directly from local device memory
    @AppStorage("username") private var savedUsername = "Guest User"
    @AppStorage("email") private var savedEmail = "No email provided"
    
    // 1. Pull the email string from AppStorage
    @AppStorage("loggedInUserEmail") private var loggedInUserEmail: String = ""
    
    // 2. Local state to hold the live text field input
    @State private var questionInput: String = ""
    @State private var questionPriceInput: Double = 0.0
    @State private var answerInput: String = ""
    
    // find the user that matches with the loggedInUserEmail
    // 2. Declare the query property (do not assign a filter here)
    @Query private var matchedUsers: [Profile]
    // 3. Create a custom initializer to read the AppStorage string
    init() {
        // Fetch the value directly from UserDefaults during initialization
        let savedEmail = UserDefaults.standard.string(forKey: "loggedInUserEmail") ?? ""
        let cleanEmail = savedEmail.lowercased().trimmingCharacters(in: .whitespacesAndNewlines)
        // Build the database search filter
        let filter = #Predicate<Profile> { profile in
            profile.email == cleanEmail
        }
        // Inject the filter back into the underlying @Query property wrapper
        _matchedUsers = Query(filter: filter)
    }
    
    @Query private var questions: [Question]
    
//    // Queries only users with the specific admin email address
//    @Query(filter: #Predicate<Question> { question in
//        question.loggedInUserEmail == loggedInUserEmail
//    })
//    private var questionsList: [Question]
    
    func submitAnswerToQuestion() {
        // 1. Build the multi-variable data instance
        let newAnswer = Answer(
            createdAt: Date(), question: "question", loggedInUserEmail: "email", answer: "answer", answerPrice: 2.0
        )
        
        // 2. Initialize the parent model object
//        let currentQuestion = Question()
        let currentQuestion = Question(loggedInUserEmail: loggedInUserEmail, price: questionPriceInput, question: "question", answers: [newAnswer])
        
        // 3. Tie the relationship elements together
        currentQuestion.answers = newAnswer
        
        // 4. Insert into context (SwiftData cascade-saves the sub-model automatically)
        modelContext.insert(currentQuestion)
        try? modelContext.save()
    }
    
//    var body: some View {
//            ScrollView {
//                VStack(spacing: 20) {
//                    Form {
//                        Section("Ask A Question") {
//                            TextField("Your question...", text: $questionInput)
//                                .textFieldStyle(.plain)
//                            TextField("Question Price", value: $questionPriceInput, format: .currency(code: "USD"))
//                                .keyboardType(.decimalPad)
//                        }
//                        Section {
//                            Button(action: submitQuestionToDatabase) {
//                                Text("Submit Your Question")
//                                    .frame(maxWidth: .infinity, alignment: .center)
//                                    .bold()
//                            }
//                            // Disable the button if the user hasn't typed anything
//                            .disabled(questionInput.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty)
//                        }
//                    }
////                    ForEach(1...30, id: \.self) { item in
////                        Text("Row Item #\(item)")
////                            .frame(maxWidth: .infinity)
////                            .padding()
////                            .background(Color.blue.opacity(0.1))
////                            .cornerRadius(8)
////                    }
//                }
//                .padding() // Adds safe padding around the inner list contents
//            }
//        }
    
    var body: some View {
        NavigationStack {
            Group {
                Form {
                    Section("Ask A Question") {
                        TextField("Your question...", text: $questionInput)
                            .textFieldStyle(.plain)
                        TextField("Question Price", value: $questionPriceInput, format: .currency(code: "USD"))
                            .keyboardType(.decimalPad)
                    }
                    
                    Section {
                        Button(action: submitQuestionToDatabase) {
                            Text("Submit Your Question")
                                .frame(maxWidth: .infinity, alignment: .center)
                                .bold()
                        }
                        // Disable the button if the user hasn't typed anything
                        .disabled(questionInput.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty)
                    }
                }
                .navigationTitle("Questions")
                List(questions) { question in
                    Text(question.loggedInUserEmail)
                        .font(Font.body.bold())
                    Text(question.question)
                }
                Form {
                    Section("Provide an Answer") {
                        TextField("Your answer...", text: $answerInput)
                            .textFieldStyle(.plain)
                    }
                    Section {
                        Button(action: submitAnswerToQuestion) {
                            Text("Submit Your Answer")
                                .frame(maxWidth: .infinity, alignment: .center)
                                .bold()
                        }
                        // Disable the button if the user hasn't typed anything
                        .disabled(answerInput.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty)
                    }
                }
                Form {
                    Button("Print All Database Users") {
                        print("--- DATABASE EXPORT (Total: \(questions.count)) ---")
                        for (index, question) in questions.enumerated() {
                            print("Index \(index): Email = \(question.loggedInUserEmail), Question = \(question.question)")
                        }
                        print("---------------------------------------")
                    }
                    
                }
            }
            .navigationTitle("Questions")
        }
    }
    // 3. The submission handler
    @MainActor
    func submitQuestionToDatabase() {
        // Clean the input text
        let cleanText = questionInput.trimmingCharacters(in: .whitespacesAndNewlines)
        
        // Ensure data isn't empty after trimming
        guard !cleanText.isEmpty else { return }
        
        // 4. Create an instance of your SwiftData @Model class
        let newQuestion = Question(loggedInUserEmail: loggedInUserEmail, price: questionPriceInput, question: cleanText, answers: [])
        
        // 5. Insert the object into the database context
        modelContext.insert(newQuestion)
        
        // 6. Force an immediate write to disk so changes aren't lost on app exit
        do {
            try modelContext.save()
            
            // 7. Reset the input field UI back to blank upon success
            questionInput = ""
            print("Saved to SwiftData!")
            
        } catch {
            print("Failed to save to SwiftData: \(error.localizedDescription)")
        }
    }
}


// QuestionsView - E

// SettingsView - B

// SettingsView - E

// SignUpView - B

// SignUpView - E

// QuestionView - B - saving Answer to Databse maybe - need to print it
import SwiftUI
import SwiftData

struct QuestionsView: View {
    // 1. Access the database context
    @Environment(\.modelContext) private var modelContext
    
    // 💡 Read the username string directly from local device memory
    @AppStorage("username") private var savedUsername = "Guest User"
    @AppStorage("email") private var savedEmail = "No email provided"
    
    // 1. Pull the email string from AppStorage
    @AppStorage("loggedInUserEmail") private var loggedInUserEmail: String = ""
    
    // 2. Local state to hold the live text field input
    @State private var questionInput: String = ""
    @State private var questionPriceInput: Double = 0.0
    @State private var answerInput: String = ""
    
    // find the user that matches with the loggedInUserEmail
    // 2. Declare the query property (do not assign a filter here)
    @Query private var matchedUsers: [Profile]
    // 3. Create a custom initializer to read the AppStorage string
    init() {
        // Fetch the value directly from UserDefaults during initialization
        let savedEmail = UserDefaults.standard.string(forKey: "loggedInUserEmail") ?? ""
        let cleanEmail = savedEmail.lowercased().trimmingCharacters(in: .whitespacesAndNewlines)
        // Build the database search filter
        let filter = #Predicate<Profile> { profile in
            profile.email == cleanEmail
        }
        // Inject the filter back into the underlying @Query property wrapper
        _matchedUsers = Query(filter: filter)
    }
    
    @Query private var questions: [Question]
    
//    // Queries only users with the specific admin email address
//    @Query(filter: #Predicate<Question> { question in
//        question.loggedInUserEmail == loggedInUserEmail
//    })
//    private var questionsList: [Question]
    
    func submitAnswerToQuestion() {
        // 1. Build the multi-variable data instance
        let newAnswer = Answer(
            createdAt: Date(), question: "question", loggedInUserEmail: "email", answer: "answer", answerPrice: 2.0
        )
        
        // 2. Initialize the parent model object
//        let currentQuestion = Question()
        let currentQuestion = Question(loggedInUserEmail: loggedInUserEmail, price: questionPriceInput, question: "question", answers: [newAnswer])
        
        // 3. Tie the relationship elements together
        currentQuestion.answers = [newAnswer]
        
        // 4. Insert into context (SwiftData cascade-saves the sub-model automatically)
        modelContext.insert(currentQuestion)
        try? modelContext.save()
    }
    
//    var body: some View {
//            ScrollView {
//                VStack(spacing: 20) {
//                    Form {
//                        Section("Ask A Question") {
//                            TextField("Your question...", text: $questionInput)
//                                .textFieldStyle(.plain)
//                            TextField("Question Price", value: $questionPriceInput, format: .currency(code: "USD"))
//                                .keyboardType(.decimalPad)
//                        }
//                        Section {
//                            Button(action: submitQuestionToDatabase) {
//                                Text("Submit Your Question")
//                                    .frame(maxWidth: .infinity, alignment: .center)
//                                    .bold()
//                            }
//                            // Disable the button if the user hasn't typed anything
//                            .disabled(questionInput.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty)
//                        }
//                    }
////                    ForEach(1...30, id: \.self) { item in
////                        Text("Row Item #\(item)")
////                            .frame(maxWidth: .infinity)
////                            .padding()
////                            .background(Color.blue.opacity(0.1))
////                            .cornerRadius(8)
////                    }
//                }
//                .padding() // Adds safe padding around the inner list contents
//            }
//        }
    
    var body: some View {
        NavigationStack {
            Group {
                Form {
                    Section("Ask A Question") {
                        TextField("Your question...", text: $questionInput)
                            .textFieldStyle(.plain)
                        TextField("Question Price", value: $questionPriceInput, format: .currency(code: "USD"))
                            .keyboardType(.decimalPad)
                    }
                    
                    Section {
                        Button(action: submitQuestionToDatabase) {
                            Text("Submit Your Question")
                                .frame(maxWidth: .infinity, alignment: .center)
                                .bold()
                        }
                        // Disable the button if the user hasn't typed anything
                        .disabled(questionInput.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty)
                    }
                }
                .navigationTitle("Questions")
                List(questions) { question in
                    Text(question.loggedInUserEmail)
                        .font(Font.body.bold())
                    Text(question.question)
                }
                Form {
                    Section("Provide an Answer") {
                        TextField("Your answer...", text: $answerInput)
                            .textFieldStyle(.plain)
                    }
                    Section {
                        Button(action: submitAnswerToQuestion) {
                            Text("Submit Your Answer")
                                .frame(maxWidth: .infinity, alignment: .center)
                                .bold()
                        }
                        // Disable the button if the user hasn't typed anything
                        .disabled(answerInput.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty)
                    }
                }
                Form {
                    Button("Print All Database Users") {
         
                        print("--- DATABASE EXPORT (Total: \(questions.count)) ---")
                        for (index, question) in questions.enumerated() {
                            print("Index \(index): Email = \(question.loggedInUserEmail), Question = \(question.question)")
                        }
                        print("---------------------------------------")
                    }
                    
                }
            }
            .navigationTitle("Questions")
        }
    }
    // 3. The submission handler
    @MainActor
    func submitQuestionToDatabase() {
        // Clean the input text
        let cleanText = questionInput.trimmingCharacters(in: .whitespacesAndNewlines)
        
        // Ensure data isn't empty after trimming
        guard !cleanText.isEmpty else { return }
        
        // 4. Create an instance of your SwiftData @Model class
        let newQuestion = Question(loggedInUserEmail: loggedInUserEmail, price: questionPriceInput, question: cleanText, answers: [])
        
        // 5. Insert the object into the database context
        modelContext.insert(newQuestion)
        
        // 6. Force an immediate write to disk so changes aren't lost on app exit
        do {
            try modelContext.save()
            
            // 7. Reset the input field UI back to blank upon success
            questionInput = ""
            print("Saved to SwiftData!")
            
        } catch {
            print("Failed to save to SwiftData: \(error.localizedDescription)")
        }
    }
}
// QestionsView - E

// Questions

import Foundation
import SwiftData


// 1. Create a conforming Codable struct
//struct Answer: Codable {
//    var id = UUID()
//    var createdAt = Date
//    var question = String
//    var userEmail: String
//    var answer: String
//    var price: Int
//}
@Model
class Answer {
    var id = UUID()
    var createdAt: Date
    var question: String
    var loggedInUserEmail: String
    var answer: String
    var answerPrice: Double
    
    init(createdAt: Date = .now, question: String, loggedInUserEmail: String, answer: String, answerPrice: Double) {
        self.id = UUID()
        self.createdAt = createdAt
        self.question = question
        self.loggedInUserEmail = loggedInUserEmail.trimmingCharacters(in: .whitespacesAndNewlines).lowercased()
        self.answer = answer
        self.answerPrice = answerPrice
    }
}

@Model
class Question {
    var id = UUID()
    var createdAt: Date
    // 1. Mark the email as unique to ensure data integrity
    var loggedInUserEmail: String
    var price: Double
    var tags: [String]
    var question: String
    
    // Establishing the relationship link to the child model
    // @Relationship(deleteRule: .cascade, inverse: \Question.answers)
    var answers: [Answer]? // Note: SwiftData relationships should be optional

    init(createdAt: Date = .now, loggedInUserEmail: String, price: Double, tags: [String] = [], question: String, answers: [Answer]) {
    // init(createdAt: Date = .now, loggedInUserEmail: String, price: Double, tags: [String] = [], question: String) {
        self.id = UUID()
        self.createdAt = createdAt
        // Clean text to prevent uppercase variations or trailing whitespace bugs
        self.loggedInUserEmail = loggedInUserEmail.trimmingCharacters(in: .whitespacesAndNewlines).lowercased()
        self.price = price
        self.tags = tags
        self.question = question
        // self.answers = answers
    }
}

// QuestionsView - B

import SwiftUI
import SwiftData

struct QuestionsView: View {
    // 1. Access the database context
    @Environment(\.modelContext) private var modelContext
    
    // 💡 Read the username string directly from local device memory
    @AppStorage("username") private var savedUsername = "Guest User"
    @AppStorage("email") private var savedEmail = "No email provided"
    
    // 1. Pull the email string from AppStorage
    @AppStorage("loggedInUserEmail") private var loggedInUserEmail: String = ""
    
    // 2. Local state to hold the live text field input
    @State private var questionInput: String = ""
    @State private var questionPriceInput: Double = 0.0
    @State private var answerInput: String = ""
    
    // find the user that matches with the loggedInUserEmail
    // 2. Declare the query property (do not assign a filter here)
    @Query private var matchedUsers: [Profile]
    // 3. Create a custom initializer to read the AppStorage string
    init() {
        // Fetch the value directly from UserDefaults during initialization
        let savedEmail = UserDefaults.standard.string(forKey: "loggedInUserEmail") ?? ""
        let cleanEmail = savedEmail.lowercased().trimmingCharacters(in: .whitespacesAndNewlines)
        // Build the database search filter
        let filter = #Predicate<Profile> { profile in
            profile.email == cleanEmail
        }
        // Inject the filter back into the underlying @Query property wrapper
        _matchedUsers = Query(filter: filter)
    }
    
    @Query private var questions: [Question]
    
//    // Queries only users with the specific admin email address
//    @Query(filter: #Predicate<Question> { question in
//        question.loggedInUserEmail == loggedInUserEmail
//    })
//    private var questionsList: [Question]
    
    func submitAnswerToQuestion() {
        // 1. Build the multi-variable data instance
        let newAnswer = Answer(
            createdAt: Date(), question: "question", loggedInUserEmail: "email", answer: "answer", answerPrice: 2.0
        )
        
        // 2. Initialize the parent model object
//        let currentQuestion = Question()
        let currentQuestion = Question(loggedInUserEmail: loggedInUserEmail, price: questionPriceInput, question: "question", answers: [newAnswer])
        
        // 3. Tie the relationship elements together
        currentQuestion.answers = [newAnswer]
        
        // 4. Insert into context (SwiftData cascade-saves the sub-model automatically)
        modelContext.insert(currentQuestion)
        try? modelContext.save()
    }
    
//    var body: some View {
//            ScrollView {
//                VStack(spacing: 20) {
//                    Form {
//                        Section("Ask A Question") {
//                            TextField("Your question...", text: $questionInput)
//                                .textFieldStyle(.plain)
//                            TextField("Question Price", value: $questionPriceInput, format: .currency(code: "USD"))
//                                .keyboardType(.decimalPad)
//                        }
//                        Section {
//                            Button(action: submitQuestionToDatabase) {
//                                Text("Submit Your Question")
//                                    .frame(maxWidth: .infinity, alignment: .center)
//                                    .bold()
//                            }
//                            // Disable the button if the user hasn't typed anything
//                            .disabled(questionInput.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty)
//                        }
//                    }
////                    ForEach(1...30, id: \.self) { item in
////                        Text("Row Item #\(item)")
////                            .frame(maxWidth: .infinity)
////                            .padding()
////                            .background(Color.blue.opacity(0.1))
////                            .cornerRadius(8)
////                    }
//                }
//                .padding() // Adds safe padding around the inner list contents
//            }
//        }
    
    var body: some View {
        NavigationStack {
            Group {
                Form {
                    Section("Ask A Question") {
                        TextField("Your question...", text: $questionInput)
                            .textFieldStyle(.plain)
                        TextField("Question Price", value: $questionPriceInput, format: .currency(code: "USD"))
                            .keyboardType(.decimalPad)
                    }
                    
                    Section {
                        Button(action: submitQuestionToDatabase) {
                            Text("Submit Your Question")
                                .frame(maxWidth: .infinity, alignment: .center)
                                .bold()
                        }
                        // Disable the button if the user hasn't typed anything
                        .disabled(questionInput.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty)
                    }
                }
                .navigationTitle("Questions")
                List(questions) { question in
                    Text(question.loggedInUserEmail)
                        .font(Font.body.bold())
                    Text(question.question)
                    // Text(question.answers?.count ?? 0)
                    if let answersList = question.answers, !answersList.isEmpty {
                        // 2. Loop through each child model instance
                        ForEach(answersList) { answer in
                            // 3. Render the specific string property of the child model
                            Text(answer.answer)
                                .padding(.vertical, 2)
                        }
                    } else {
                        Text("No answers submitted yet.")
                            .foregroundColor(.secondary)
                    }
                }
                Form {
                    Section("Provide an Answer") {
                        TextField("Your answer...", text: $answerInput)
                            .textFieldStyle(.plain)
                    }
                    Section {
                        Button(action: submitAnswerToQuestion) {
                            Text("Submit Your Answer")
                                .frame(maxWidth: .infinity, alignment: .center)
                                .bold()
                        }
                        // Disable the button if the user hasn't typed anything
                        .disabled(answerInput.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty)
                    }
                }
                Form {
                    Button("Print All Database Users") {
         
                        print("--- DATABASE EXPORT (Total: \(questions.count)) ---")
                        for (index, question) in questions.enumerated() {
//                            print("Index \(index): Email = \(question.loggedInUserEmail), Question = \(question.question)")
//                            print("Index \(index): Email = \(question.loggedInUserEmail), Question = \(question.question), Answer = \(profile.answers?.count ?? 0)")
//                            print("Index \(index): Email = \(question.loggedInUserEmail), Question = \(question.question), Answer = \(question.answers ?? [])")
                            print("Index \(index): Email = \(question.loggedInUserEmail), Question = \(question.question), Answer = \(question.answers?.first?.answer ?? "No response yet")")
                        }
                        print("---------------------------------------")
                    }
                    
                }
            }
            .navigationTitle("Questions")
        }
    }
//    func debugPrintAnswers() {
//        // 1. Fallback to an empty array if the relationship is nil
//        let answersList = question.answers ?? []
//        
//        // 2. Loop through and print the string property of each child model
//        for answer in answersList {
//            print("Answer text entry: \(answer.answerText)")
//        }
//    }
    // 3. The submission handler
    @MainActor
    func submitQuestionToDatabase() {
        // Clean the input text
        let cleanText = questionInput.trimmingCharacters(in: .whitespacesAndNewlines)
        
        // Ensure data isn't empty after trimming
        guard !cleanText.isEmpty else { return }
        
        // 4. Create an instance of your SwiftData @Model class
        let newQuestion = Question(loggedInUserEmail: loggedInUserEmail, price: questionPriceInput, question: cleanText, answers: [])
        
        // 5. Insert the object into the database context
        modelContext.insert(newQuestion)
        
        // 6. Force an immediate write to disk so changes aren't lost on app exit
        do {
            try modelContext.save()
            
            // 7. Reset the input field UI back to blank upon success
            questionInput = ""
            print("Saved to SwiftData!")
            
        } catch {
            print("Failed to save to SwiftData: \(error.localizedDescription)")
        }
    }
}


// QuestionsView - E

// Question - B

@Model
class Answer {
    var id = UUID()
    var createdAt: Date
    var question: String
    var loggedInUserEmail: String
    var answer: String
    var answerPrice: Double
    
    init(createdAt: Date = .now, question: String, loggedInUserEmail: String, answer: String, answerPrice: Double) {
        self.id = UUID()
        self.createdAt = createdAt
        self.question = question
        self.loggedInUserEmail = loggedInUserEmail.trimmingCharacters(in: .whitespacesAndNewlines).lowercased()
        self.answer = answer
        self.answerPrice = answerPrice
    }
}

@Model
class Question {
    var id = UUID()
    var createdAt: Date
    // 1. Mark the email as unique to ensure data integrity
    var loggedInUserEmail: String
    var price: Double
    var tags: [String]
    var question: String
    
    // Establishing the relationship link to the child model
    // @Relationship(deleteRule: .cascade, inverse: \Question.answers)
    var answers: [Answer]? // Note: SwiftData relationships should be optional

    init(createdAt: Date = .now, loggedInUserEmail: String, price: Double, tags: [String] = [], question: String, answers: [Answer]) {
    // init(createdAt: Date = .now, loggedInUserEmail: String, price: Double, tags: [String] = [], question: String) {
        self.id = UUID()
        self.createdAt = createdAt
        // Clean text to prevent uppercase variations or trailing whitespace bugs
        self.loggedInUserEmail = loggedInUserEmail.trimmingCharacters(in: .whitespacesAndNewlines).lowercased()
        self.price = price
        self.tags = tags
        self.question = question
        self.answers = answers
    }
}

// Question - E


// Question - B

import Foundation
import SwiftData


// 1. Create a conforming Codable struct
//struct Answer: Codable {
//    var id = UUID()
//    var createdAt = Date
//    var question = String
//    var userEmail: String
//    var answer: String
//    var price: Int
//}

@Model
class Question {
    var id = UUID()
    var createdAt: Date
    // 1. Mark the email as unique to ensure data integrity
    var loggedInUserEmail: String
    var price: Double
    var tags: [String]
    var question: String
    
    // Establishing the relationship link to the child model
    // @Relationship(deleteRule: .cascade, inverse: \Question.answers)
    @Relationship(deleteRule: .cascade) var answers: [Answer]?
    // var answers: [Answer]? // Note: SwiftData relationships should be optional

    init(createdAt: Date = .now, loggedInUserEmail: String, price: Double, tags: [String] = [], question: String, answers: [Answer]) {
    // init(createdAt: Date = .now, loggedInUserEmail: String, price: Double, tags: [String] = [], question: String) {
        self.id = UUID()
        self.createdAt = createdAt
        // Clean text to prevent uppercase variations or trailing whitespace bugs
        self.loggedInUserEmail = loggedInUserEmail.trimmingCharacters(in: .whitespacesAndNewlines).lowercased()
        self.price = price
        self.tags = tags
        self.question = question
        self.answers = []
    }
}

@Model
class Answer {
    var id = UUID()
    var createdAt: Date
    var question: String
    var loggedInUserEmail: String
    var answer: String
    var answerPrice: Double
    
    init(createdAt: Date = .now, question: String, loggedInUserEmail: String, answer: String, answerPrice: Double) {
        self.id = UUID()
        self.createdAt = createdAt
        self.question = question
        self.loggedInUserEmail = loggedInUserEmail.trimmingCharacters(in: .whitespacesAndNewlines).lowercased()
        self.answer = answer
        self.answerPrice = answerPrice
    }
}

// Question - E

// QuestionsView - B

import SwiftUI
import SwiftData

struct QuestionsView: View {
    // 1. Access the database context
    @Environment(\.modelContext) private var modelContext
    
    // 💡 Read the username string directly from local device memory
    @AppStorage("username") private var savedUsername = "Guest User"
    @AppStorage("email") private var savedEmail = "No email provided"
    
    // 1. Pull the email string from AppStorage
    @AppStorage("loggedInUserEmail") private var loggedInUserEmail: String = ""
    
    // 2. Local state to hold the live text field input
    @State private var questionInput: String = ""
    @State private var questionPriceInput: Double = 0.0
    @State private var answerInput: String = ""
    
    // find the user that matches with the loggedInUserEmail
    // 2. Declare the query property (do not assign a filter here)
    @Query private var matchedUsers: [Profile]
    // 3. Create a custom initializer to read the AppStorage string
    init() {
        // Fetch the value directly from UserDefaults during initialization
        let savedEmail = UserDefaults.standard.string(forKey: "loggedInUserEmail") ?? ""
        let cleanEmail = savedEmail.lowercased().trimmingCharacters(in: .whitespacesAndNewlines)
        // Build the database search filter
        let filter = #Predicate<Profile> { profile in
            profile.email == cleanEmail
        }
        // Inject the filter back into the underlying @Query property wrapper
        _matchedUsers = Query(filter: filter)
    }
    
    @Query private var questions: [Question]
    
//    // Queries only users with the specific admin email address
//    @Query(filter: #Predicate<Question> { question in
//        question.loggedInUserEmail == loggedInUserEmail
//    })
//    private var questionsList: [Question]
    
    func submitAnswerToQuestion() {
        // 1. Build the multi-variable data instance
        let newAnswer = Answer(
            createdAt: Date(), question: "question", loggedInUserEmail: "email", answer: "answer", answerPrice: 2.0
        )
        
        // 2. Initialize the parent model object
//        let currentQuestion = Question()
        let currentQuestion = Question(loggedInUserEmail: loggedInUserEmail, price: questionPriceInput, question: "question", answers: [newAnswer])
        
        // 3. Tie the relationship elements together
        currentQuestion.answers = [newAnswer]
        
        // 4. Insert into context (SwiftData cascade-saves the sub-model automatically)
        modelContext.insert(currentQuestion)
        try? modelContext.save()
    }
    
//    var body: some View {
//            ScrollView {
//                VStack(spacing: 20) {
//                    Form {
//                        Section("Ask A Question") {
//                            TextField("Your question...", text: $questionInput)
//                                .textFieldStyle(.plain)
//                            TextField("Question Price", value: $questionPriceInput, format: .currency(code: "USD"))
//                                .keyboardType(.decimalPad)
//                        }
//                        Section {
//                            Button(action: submitQuestionToDatabase) {
//                                Text("Submit Your Question")
//                                    .frame(maxWidth: .infinity, alignment: .center)
//                                    .bold()
//                            }
//                            // Disable the button if the user hasn't typed anything
//                            .disabled(questionInput.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty)
//                        }
//                    }
////                    ForEach(1...30, id: \.self) { item in
////                        Text("Row Item #\(item)")
////                            .frame(maxWidth: .infinity)
////                            .padding()
////                            .background(Color.blue.opacity(0.1))
////                            .cornerRadius(8)
////                    }
//                }
//                .padding() // Adds safe padding around the inner list contents
//            }
//        }
    
    var body: some View {
        NavigationStack {
            Group {
                Form {
                    Section("Ask A Question") {
                        TextField("Your question...", text: $questionInput)
                            .textFieldStyle(.plain)
                        TextField("Question Price", value: $questionPriceInput, format: .currency(code: "USD"))
                            .keyboardType(.decimalPad)
                    }
                    
                    Section {
                        Button(action: submitQuestionToDatabase) {
                            Text("Submit Your Question")
                                .frame(maxWidth: .infinity, alignment: .center)
                                .bold()
                        }
                        // Disable the button if the user hasn't typed anything
                        .disabled(questionInput.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty)
                    }
                }
                .navigationTitle("Questions")
                List(questions) { question in
                    Text(question.loggedInUserEmail)
                        .font(Font.body.bold())
                    Text(question.question)
                    // Text(question.answers?.count ?? 0)
//                    if let answersList = question.answers, !answersList.isEmpty {
//                        // 2. Loop through each child model instance
//                        ForEach(answersList) { answer in
//                            // 3. Render the specific string property of the child model
//                            Text(answer.question)
//                                .padding(.vertical, 2)
//                        }
//                    } else {
//                        Text("No answers submitted yet.")
//                            .foregroundColor(.secondary)
//                    }
                    Section("Answers List") {
                        // 2. Safely unwrap the optional relationship array
                        if let answersList = question.answers, !answersList.isEmpty {
                            // 3. Loop through the array of child models
                            ForEach(answersList) { answer in
                                HStack {
                                    Image(systemName: "arrow.turn.down.right")
                                        .foregroundColor(.gray)
                                    
                                    // 4. ✅ CRITICAL: Pluck the specific string variable out of the child object
                                    Text(answer.answer)
                                }
                            }
                        } else {
                            Text("No answers provided yet.")
                                .foregroundColor(.secondary)
                        }
                    }
                }
                Form {
                    Section("Provide an Answer") {
                        TextField("Your answer...", text: $answerInput)
                            .textFieldStyle(.plain)
                    }
                    Section {
                        Button(action: submitAnswerToQuestion) {
                            Text("Submit Your Answer")
                                .frame(maxWidth: .infinity, alignment: .center)
                                .bold()
                        }
                        // Disable the button if the user hasn't typed anything
                        .disabled(answerInput.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty)
                    }
                }
                Form {
                    Button("Print All Database Users") {
         
                        print("--- DATABASE EXPORT (Total: \(questions.count)) ---")
                        for (index, question) in questions.enumerated() {
//                            print("Index \(index): Email = \(question.loggedInUserEmail), Question = \(question.question)")
//                            print("Index \(index): Email = \(question.loggedInUserEmail), Question = \(question.question), Answer = \(profile.answers?.count ?? 0)")
//                            print("Index \(index): Email = \(question.loggedInUserEmail), Question = \(question.question), Answer = \(question.answers ?? [])")
                            print("Index \(index): Email = \(question.loggedInUserEmail), Question = \(question.question), Answer = \(question.answers?.first?.answer ?? "No response yet")")
                        }
                        print("---------------------------------------")
                    }
                    
                }
            }
            .navigationTitle("Questions")
        }
    }
//    func debugPrintAnswers() {
//        // 1. Fallback to an empty array if the relationship is nil
//        let answersList = question.answers ?? []
//        
//        // 2. Loop through and print the string property of each child model
//        for answer in answersList {
//            print("Answer text entry: \(answer.answerText)")
//        }
//    }
    // 3. The submission handler
    @MainActor
    func submitQuestionToDatabase() {
        // Clean the input text
        let cleanText = questionInput.trimmingCharacters(in: .whitespacesAndNewlines)
        
        // Ensure data isn't empty after trimming
        guard !cleanText.isEmpty else { return }
        
        // 4. Create an instance of your SwiftData @Model class
        let newQuestion = Question(loggedInUserEmail: loggedInUserEmail, price: questionPriceInput, question: cleanText, answers: [])
        
        // 5. Insert the object into the database context
        modelContext.insert(newQuestion)
        
        // 6. Force an immediate write to disk so changes aren't lost on app exit
        do {
            try modelContext.save()
            
            // 7. Reset the input field UI back to blank upon success
            questionInput = ""
            print("Saved to SwiftData!")
            
        } catch {
            print("Failed to save to SwiftData: \(error.localizedDescription)")
        }
    }
}


// QuestionsView - E

// QuestionsView - B

import SwiftUI
import SwiftData

struct QuestionsView: View {
    // 1. Access the database context
    @Environment(\.modelContext) private var modelContext
    
    // 💡 Read the username string directly from local device memory
    @AppStorage("username") private var savedUsername = "Guest User"
    @AppStorage("email") private var savedEmail = "No email provided"
    
    // 1. Pull the email string from AppStorage
    @AppStorage("loggedInUserEmail") private var loggedInUserEmail: String = ""
    
    // 2. Local state to hold the live text field input
    @State private var questionInput: String = ""
    @State private var questionPriceInput: Double = 0.0
    @State private var answerInput: String = ""
    
    // find the user that matches with the loggedInUserEmail
    // 2. Declare the query property (do not assign a filter here)
    @Query private var matchedUsers: [Profile]
    // 3. Create a custom initializer to read the AppStorage string
    init() {
        // Fetch the value directly from UserDefaults during initialization
        let savedEmail = UserDefaults.standard.string(forKey: "loggedInUserEmail") ?? ""
        let cleanEmail = savedEmail.lowercased().trimmingCharacters(in: .whitespacesAndNewlines)
        // Build the database search filter
        let filter = #Predicate<Profile> { profile in
            profile.email == cleanEmail
        }
        // Inject the filter back into the underlying @Query property wrapper
        _matchedUsers = Query(filter: filter)
    }
    
    @Query private var questions: [Question]
    
//    // Queries only users with the specific admin email address
//    @Query(filter: #Predicate<Question> { question in
//        question.loggedInUserEmail == loggedInUserEmail
//    })
//    private var questionsList: [Question]
    
    func submitAnswerToQuestion() {
        // 1. Build the multi-variable data instance
        let newAnswer = Answer(
            createdAt: Date(), question: questionInput, loggedInUserEmail: loggedInUserEmail, answer: answerInput, answerPrice: 0.0
        )
        
        // 2. Initialize the parent model object
//        let currentQuestion = Question()
        let currentQuestion = Question(loggedInUserEmail: loggedInUserEmail, price: questionPriceInput, question: questionInput, answers: [newAnswer])
        
        // 3. Tie the relationship elements together
        currentQuestion.answers = [newAnswer]
        
        // 4. Insert into context (SwiftData cascade-saves the sub-model automatically)
        modelContext.insert(currentQuestion)
        try? modelContext.save()
    }
    
//    var body: some View {
//            ScrollView {
//                VStack(spacing: 20) {
//                    Form {
//                        Section("Ask A Question") {
//                            TextField("Your question...", text: $questionInput)
//                                .textFieldStyle(.plain)
//                            TextField("Question Price", value: $questionPriceInput, format: .currency(code: "USD"))
//                                .keyboardType(.decimalPad)
//                        }
//                        Section {
//                            Button(action: submitQuestionToDatabase) {
//                                Text("Submit Your Question")
//                                    .frame(maxWidth: .infinity, alignment: .center)
//                                    .bold()
//                            }
//                            // Disable the button if the user hasn't typed anything
//                            .disabled(questionInput.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty)
//                        }
//                    }
////                    ForEach(1...30, id: \.self) { item in
////                        Text("Row Item #\(item)")
////                            .frame(maxWidth: .infinity)
////                            .padding()
////                            .background(Color.blue.opacity(0.1))
////                            .cornerRadius(8)
////                    }
//                }
//                .padding() // Adds safe padding around the inner list contents
//            }
//        }
    
    var body: some View {
        NavigationStack {
            Group {
                Form {
                    Section("Ask A Question") {
                        TextField("Your question...", text: $questionInput)
                            .textFieldStyle(.plain)
                        TextField("Question Price", value: $questionPriceInput, format: .currency(code: "USD"))
                            .keyboardType(.decimalPad)
                    }
                    
                    Section {
                        Button(action: submitQuestionToDatabase) {
                            Text("Submit Your Question")
                                .frame(maxWidth: .infinity, alignment: .center)
                                .bold()
                        }
                        // Disable the button if the user hasn't typed anything
                        .disabled(questionInput.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty)
                    }
                }
                .navigationTitle("Questions")
                List(questions) { question in
                    Text(question.loggedInUserEmail)
                        .font(Font.body.bold())
                    Text(question.question)
                    // Text(question.answers?.count ?? 0)
//                    if let answersList = question.answers, !answersList.isEmpty {
//                        // 2. Loop through each child model instance
//                        ForEach(answersList) { answer in
//                            // 3. Render the specific string property of the child model
//                            Text(answer.question)
//                                .padding(.vertical, 2)
//                        }
//                    } else {
//                        Text("No answers submitted yet.")
//                            .foregroundColor(.secondary)
//                    }
                    Section("Answers List") {
                        // 2. Safely unwrap the optional relationship array
                        if let answersList = question.answers, !answersList.isEmpty {
                            // 3. Loop through the array of child models
                            ForEach(answersList) { answer in
                                HStack {
                                    Image(systemName: "arrow.turn.down.right")
                                        .foregroundColor(.gray)
                                    
                                    // 4. ✅ CRITICAL: Pluck the specific string variable out of the child object
                                    Text(answer.answer)
                                }
                            }
                        } else {
                            Text("No answers provided yet.")
                                .foregroundColor(.secondary)
                        }
                    }
                }
                Form {
                    Section("Provide an Answer") {
                        TextField("Your answer...", text: $answerInput)
                            .textFieldStyle(.plain)
                    }
                    Section {
                        Button(action: submitAnswerToQuestion) {
                            Text("Submit Your Answer")
                                .frame(maxWidth: .infinity, alignment: .center)
                                .bold()
                        }
                        // Disable the button if the user hasn't typed anything
                        .disabled(answerInput.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty)
                    }
                }
                Form {
                    Button("Print All Database Users") {
         
                        print("--- DATABASE EXPORT (Total: \(questions.count)) ---")
                        for (index, question) in questions.enumerated() {
//                            print("Index \(index): Email = \(question.loggedInUserEmail), Question = \(question.question)")
//                            print("Index \(index): Email = \(question.loggedInUserEmail), Question = \(question.question), Answer = \(profile.answers?.count ?? 0)")
//                            print("Index \(index): Email = \(question.loggedInUserEmail), Question = \(question.question), Answer = \(question.answers ?? [])")
                            print("Index \(index): Email = \(question.loggedInUserEmail), Question = \(question.question), Answer = \(question.answers?.first?.answer ?? "No response yet")")
                            print(question.debugSummaryQuestion)
//                            print(question.answers?.first?.answer.debugSummaryAnswer ?? "no response yet")
                        }
                        print("---------------------------------------")
                    }
                    
                }
            }
            .navigationTitle("Questions")
        }
    }
//    func debugPrintAnswers() {
//        // 1. Fallback to an empty array if the relationship is nil
//        let answersList = question.answers ?? []
//        
//        // 2. Loop through and print the string property of each child model
//        for answer in answersList {
//            print("Answer text entry: \(answer.answerText)")
//        }
//    }
    // 3. The submission handler
    @MainActor
    func submitQuestionToDatabase() {
        // Clean the input text
        let cleanText = questionInput.trimmingCharacters(in: .whitespacesAndNewlines)
        
        // Ensure data isn't empty after trimming
        guard !cleanText.isEmpty else { return }
        
        // 4. Create an instance of your SwiftData @Model class
        let newQuestion = Question(loggedInUserEmail: loggedInUserEmail, price: questionPriceInput, question: cleanText, answers: [])
        
        // 5. Insert the object into the database context
        modelContext.insert(newQuestion)
        
        // 6. Force an immediate write to disk so changes aren't lost on app exit
        do {
            try modelContext.save()
            
            // 7. Reset the input field UI back to blank upon success
            questionInput = ""
            print("Saved to SwiftData!")
            
        } catch {
            print("Failed to save to SwiftData: \(error.localizedDescription)")
        }
    }
}


// QuestionsView - E

// Question - B

import Foundation
import SwiftData


// 1. Create a conforming Codable struct
//struct Answer: Codable {
//    var id = UUID()
//    var createdAt = Date
//    var question = String
//    var userEmail: String
//    var answer: String
//    var price: Int
//}

@Model
class Question {
    var id = UUID()
    var createdAt: Date
    // 1. Mark the email as unique to ensure data integrity
    var loggedInUserEmail: String
    var price: Double
    var tags: [String]
    var question: String
    
    // Establishing the relationship link to the child model
    // @Relationship(deleteRule: .cascade, inverse: \Question.answers)
    @Relationship(deleteRule: .cascade) var answers: [Answer]?
    // var answers: [Answer]? // Note: SwiftData relationships should be optional

    init(createdAt: Date = .now, loggedInUserEmail: String, price: Double, tags: [String] = [], question: String, answers: [Answer]) {
    // init(createdAt: Date = .now, loggedInUserEmail: String, price: Double, tags: [String] = [], question: String) {
        self.id = UUID()
        self.createdAt = createdAt
        // Clean text to prevent uppercase variations or trailing whitespace bugs
        self.loggedInUserEmail = loggedInUserEmail.trimmingCharacters(in: .whitespacesAndNewlines).lowercased()
        self.price = price
        self.tags = tags
        self.question = question
        self.answers = answers
    }
    
    // ✅ ADD THIS: A reusable helper to print the instance state cleanly
    var debugSummaryQuestion: String {
        """
        --- 📦 @Model UserProfile Debug ---
        Email: \(loggedInUserEmail)
        Question:    \(question)
        Answers:   \(answers)
        ----------------------------------
        """
    }
}

@Model
class Answer {
    var id = UUID()
    var createdAt: Date
    var question: String
    var loggedInUserEmail: String
    var answer: String
    var answerPrice: Double
    
    init(createdAt: Date = .now, question: String, loggedInUserEmail: String, answer: String, answerPrice: Double) {
        self.id = UUID()
        self.createdAt = createdAt
        self.question = question
        self.loggedInUserEmail = loggedInUserEmail.trimmingCharacters(in: .whitespacesAndNewlines).lowercased()
        self.answer = answer
        self.answerPrice = answerPrice
    }
    
    // ✅ ADD THIS: A reusable helper to print the instance state cleanly
    var debugSummaryAnswer: String {
        """
        --- 📦 @Model UserProfile Debug ---
        Question: \(question)
        Email:    \(loggedInUserEmail)
        Answer:   \(answer)
        ----------------------------------
        """
    }
}


// Question - E


// QuestionsView - B - almost working 

import SwiftUI
import SwiftData

struct QuestionsView: View {
    // 1. Access the database context
    @Environment(\.modelContext) private var modelContext
    
    // 💡 Read the username string directly from local device memory
    @AppStorage("username") private var savedUsername = "Guest User"
    @AppStorage("email") private var savedEmail = "No email provided"
    
    // 1. Pull the email string from AppStorage
    @AppStorage("loggedInUserEmail") private var loggedInUserEmail: String = ""
    
    // 2. Local state to hold the live text field input
    @State private var questionInput: String = ""
    @State private var questionPriceInput: Double = 0.0
    @State private var answerInput: String = ""
    
    // find the user that matches with the loggedInUserEmail
    // 2. Declare the query property (do not assign a filter here)
    @Query private var matchedUsers: [Profile]
    // 3. Create a custom initializer to read the AppStorage string
    init() {
        // Fetch the value directly from UserDefaults during initialization
        let savedEmail = UserDefaults.standard.string(forKey: "loggedInUserEmail") ?? ""
        let cleanEmail = savedEmail.lowercased().trimmingCharacters(in: .whitespacesAndNewlines)
        // Build the database search filter
        let filter = #Predicate<Profile> { profile in
            profile.email == cleanEmail
        }
        // Inject the filter back into the underlying @Query property wrapper
        _matchedUsers = Query(filter: filter)
    }
    
    @Query private var questions: [Question]
    
//    // Queries only users with the specific admin email address
//    @Query(filter: #Predicate<Question> { question in
//        question.loggedInUserEmail == loggedInUserEmail
//    })
//    private var questionsList: [Question]
    
    func submitAnswerToQuestion() {
        // 1. Build the multi-variable data instance
        let newAnswer = Answer(
            createdAt: Date(), question: questionInput, loggedInUserEmail: loggedInUserEmail, answer: answerInput, answerPrice: 0.0
        )
        
        // 2. Initialize the parent model object
//        let currentQuestion = Question()
        let currentQuestion = Question(loggedInUserEmail: loggedInUserEmail, price: questionPriceInput, question: questionInput, answers: [newAnswer])
        
        // 3. Tie the relationship elements together
        currentQuestion.answers = [newAnswer]
        
        // 4. Insert into context (SwiftData cascade-saves the sub-model automatically)
        modelContext.insert(currentQuestion)
        try? modelContext.save()
    }
    
//    var body: some View {
//            ScrollView {
//                VStack(spacing: 20) {
//                    Form {
//                        Section("Ask A Question") {
//                            TextField("Your question...", text: $questionInput)
//                                .textFieldStyle(.plain)
//                            TextField("Question Price", value: $questionPriceInput, format: .currency(code: "USD"))
//                                .keyboardType(.decimalPad)
//                        }
//                        Section {
//                            Button(action: submitQuestionToDatabase) {
//                                Text("Submit Your Question")
//                                    .frame(maxWidth: .infinity, alignment: .center)
//                                    .bold()
//                            }
//                            // Disable the button if the user hasn't typed anything
//                            .disabled(questionInput.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty)
//                        }
//                    }
////                    ForEach(1...30, id: \.self) { item in
////                        Text("Row Item #\(item)")
////                            .frame(maxWidth: .infinity)
////                            .padding()
////                            .background(Color.blue.opacity(0.1))
////                            .cornerRadius(8)
////                    }
//                }
//                .padding() // Adds safe padding around the inner list contents
//            }
//        }
    
    var body: some View {
        NavigationStack {
            Form {
                Section("Ask A Question") {
                    TextField("Your question...", text: $questionInput)
                        .textFieldStyle(.plain)
                    TextField("Question Price", value: $questionPriceInput, format: .currency(code: "USD"))
                        .keyboardType(.decimalPad)
                }
                Section {
                    Button(action: submitQuestionToDatabase) {
                        Text("Submit Your Question")
                            .frame(maxWidth: .infinity, alignment: .center)
                            .bold()
                    }
                    // Disable the button if the user hasn't typed anything
                    .disabled(questionInput.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty)
                }
            }
            .navigationTitle("Questions")
            List(questions) { question in
                Text(question.loggedInUserEmail)
                    .font(Font.body.bold())
                Text(question.question)
                Section("Answers List") {
                    // 2. Safely unwrap the optional relationship array
                    if let answersList = question.answers, !answersList.isEmpty {
                        // 3. Loop through the array of child models
                        ForEach(answersList) { answer in
                            HStack {
                                Image(systemName: "arrow.turn.down.right")
                                    .foregroundColor(.gray)
                                
                                // 4. ✅ CRITICAL: Pluck the specific string variable out of the child object
                                Text(answer.answer)
                            }
                        }
                    } else {
                        Text("No answers provided yet.")
                            .foregroundColor(.secondary)
                    }
                }
            }
            Form {
                Section("Provide an Answer") {
                    TextField("Your answer...", text: $answerInput)
                        .textFieldStyle(.plain)
                }
                Section {
                    Button(action: submitAnswerToQuestion) {
                        Text("Submit Your Answer")
                            .frame(maxWidth: .infinity, alignment: .center)
                            .bold()
                    }
                    // Disable the button if the user hasn't typed anything
                    .disabled(answerInput.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty)
                }
            }
            Form {
                Button("Print All Database Users") {
                    
                    print("--- DATABASE EXPORT (Total: \(questions.count)) ---")
                    for (index, question) in questions.enumerated() {
                        //                            print("Index \(index): Email = \(question.loggedInUserEmail), Question = \(question.question)")
                        //                            print("Index \(index): Email = \(question.loggedInUserEmail), Question = \(question.question), Answer = \(profile.answers?.count ?? 0)")
                        //                            print("Index \(index): Email = \(question.loggedInUserEmail), Question = \(question.question), Answer = \(question.answers ?? [])")
                        print("Index \(index): Email = \(question.loggedInUserEmail), Question = \(question.question), Answer = \(question.answers?.first?.answer ?? "No response yet")")
                        print(question.debugSummaryQuestion)
                        //                            print(question.answers?.first?.answer.debugSummaryAnswer ?? "no response yet")
                    }
                    print("---------------------------------------")
                }
                
            }
        }
    }
//    func debugPrintAnswers() {
//        // 1. Fallback to an empty array if the relationship is nil
//        let answersList = question.answers ?? []
//        
//        // 2. Loop through and print the string property of each child model
//        for answer in answersList {
//            print("Answer text entry: \(answer.answerText)")
//        }
//    }
    // 3. The submission handler
    @MainActor
    func submitQuestionToDatabase() {
        // Clean the input text
        let cleanText = questionInput.trimmingCharacters(in: .whitespacesAndNewlines)
        
        // Ensure data isn't empty after trimming
        guard !cleanText.isEmpty else { return }
        
        // 4. Create an instance of your SwiftData @Model class
        let newQuestion = Question(loggedInUserEmail: loggedInUserEmail, price: questionPriceInput, question: cleanText, answers: [])
        
        // 5. Insert the object into the database context
        modelContext.insert(newQuestion)
        
        // 6. Force an immediate write to disk so changes aren't lost on app exit
        do {
            try modelContext.save()
            
            // 7. Reset the input field UI back to blank upon success
            questionInput = ""
            print("Saved to SwiftData!")
            
        } catch {
            print("Failed to save to SwiftData: \(error.localizedDescription)")
        }
    }
}


// QuestionsView - E

// Question - B

import Foundation
import SwiftData


// 1. Create a conforming Codable struct
//struct Answer: Codable {
//    var id = UUID()
//    var createdAt = Date
//    var question = String
//    var userEmail: String
//    var answer: String
//    var price: Int
//}

@Model
class Question {
    var id = UUID()
    var createdAt: Date
    // 1. Mark the email as unique to ensure data integrity
    var loggedInUserEmail: String
    var price: Double
    var tags: [String]
    var question: String
    
    // Establishing the relationship link to the child model
    // @Relationship(deleteRule: .cascade, inverse: \Question.answers)
    @Relationship(deleteRule: .cascade) var answers: [Answer]?
    // var answers: [Answer]? // Note: SwiftData relationships should be optional

    init(createdAt: Date = .now, loggedInUserEmail: String, price: Double, tags: [String] = [], question: String, answers: [Answer]) {
    // init(createdAt: Date = .now, loggedInUserEmail: String, price: Double, tags: [String] = [], question: String) {
        self.id = UUID()
        self.createdAt = createdAt
        // Clean text to prevent uppercase variations or trailing whitespace bugs
        self.loggedInUserEmail = loggedInUserEmail.trimmingCharacters(in: .whitespacesAndNewlines).lowercased()
        self.price = price
        self.tags = tags
        self.question = question
        self.answers = answers
    }
    
    // ✅ ADD THIS: A reusable helper to print the instance state cleanly
    var debugSummaryQuestion: String {
        """
        --- 📦 @Model UserProfile Debug ---
        Email: \(loggedInUserEmail)
        Question:    \(question)
        Answers:   \(answers)
        ----------------------------------
        """
    }
}

@Model
class Answer {
    var id = UUID()
    var createdAt: Date
    var question: String
    var loggedInUserEmail: String
    var answer: String
    var answerPrice: Double
    
    init(createdAt: Date = .now, question: String, loggedInUserEmail: String, answer: String, answerPrice: Double) {
        self.id = UUID()
        self.createdAt = createdAt
        self.question = question
        self.loggedInUserEmail = loggedInUserEmail.trimmingCharacters(in: .whitespacesAndNewlines).lowercased()
        self.answer = answer
        self.answerPrice = answerPrice
    }
    
    // ✅ ADD THIS: A reusable helper to print the instance state cleanly
    var debugSummaryAnswer: String {
        """
        --- 📦 @Model UserProfile Debug ---
        Question: \(question)
        Email:    \(loggedInUserEmail)
        Answer:   \(answer)
        ----------------------------------
        """
    }
}


// Question - E

